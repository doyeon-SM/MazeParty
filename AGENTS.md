# MazeParty 프로젝트 작업 규칙

이 파일의 규칙은 `MazeParty` 저장소에서 시작하는 모든 새 Codex 채팅과 작업에 적용한다.

## 저장소 및 Unity 프로젝트 기준

- 이 게임 프로젝트의 Git 저장소 루트는 `C:/Unity/MazeParty`다.
- Git 상태 확인, diff, 커밋 및 이력 조회는 반드시 이 저장소를 기준으로 수행한다.
- Unity 프로젝트 루트도 `C:/Unity/MazeParty`이며 Assets 경로는 `C:/Unity/MazeParty/Assets`다.
- 별도의 보조 작업 폴더를 이 게임의 Git 저장소로 간주하지 않는다.

## 기획 임시기억 MD 규칙

- 구현 중이거나 요청받아 대화 중인 기획은 저장소 루트의 `PROJECT_MEMORY.md`에 임시로 기록한다.
- 확정된 기획 변경, 현재 구현 기준, 미정 질문과 TODO를 `PROJECT_MEMORY.md`에서 갱신한다.
- 작업 중에는 필요한 기존 정보만 Notion에서 조회하며 기획서나 회의록을 수정하지 않는다.
- 구현 중 번복되거나 폐기된 중간안은 MD에서 정리하고, Notion에는 사용자가 별도로 요청하지 않는 한 옮기지 않는다.
- 사용자의 최신 명시적 지시와 `PROJECT_MEMORY.md`가 충돌하면 사용자 지시를 우선하고 MD를 바로잡는다.

## Notion 및 회의록 기록 규칙

- 연결된 Notion의 기획서는 개인 페이지 `황도연_기획서 모음`에 있는 `재밌을것같은 4인 주사위게임` 페이지다.
  - 기획서 URL: `https://app.notion.com/p/3d3c227fda0380f6a34df09eff208ac0`
- 회의록은 기획서 하위의 `프로젝트 회의록` 데이터베이스에 저장한다.
  - 데이터베이스 URL: `https://app.notion.com/p/f8a0bc24176e4f34804f7a605234ff8f`
  - 데이터 소스: `collection://f17625ac-cf65-4c63-85f7-184f257c270f`
  - 기본 보기: `날짜별 회의록` 캘린더
- 모든 구현이 끝난 뒤 사용자가 `회의록을 작성해줘` 또는 같은 의미로 명시적으로 요청했을 때만 Notion을 수정한다.
- 회의록 요청 시 `PROJECT_MEMORY.md`의 최종 기획과 성공한 Git 커밋의 해시, 제목, 본문 및 실제 변경 내용을 함께 확인한다.
- 위 근거를 사용해 기획서에는 최종 확정안만 반영하고, 해당 날짜의 일일 회의록에는 새 기획 변경과 구현·커밋 결과를 기록한다.
- MCP 작업 중간 상태, 개별 명령 내역, 커밋에 포함되지 않은 추측성 구현 내용은 Notion에 기록하지 않는다.
- Git 커밋 성공만으로는 Notion을 자동 수정하지 않는다.
- 같은 날짜에는 `YYYY-MM-DD 회의록` 하나만 사용하며 기존 페이지가 있으면 새 내용을 누적한다.
- 기본 기록일은 `Asia/Seoul` 기준 작업일 또는 대상 커밋 날짜로 하되, 사용자가 날짜를 명시하면 그 날짜를 우선한다.
- Notion을 쓰기 전에 대상 페이지와 데이터베이스를 조회해 연결 상태와 접근 권한을 확인한다. 쓰기에 실패하면 즉시 알리고 완료한 것처럼 말하지 않는다.
