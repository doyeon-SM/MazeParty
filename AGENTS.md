# MazeParty 프로젝트 작업 규칙

이 파일의 규칙은 `MazeParty` 저장소에서 시작하는 모든 새 Codex 채팅과 작업에 적용한다.

## 저장소 및 Unity 프로젝트 기준

- 이 게임 프로젝트의 Git 저장소 루트는 `C:/Unity/MazeParty`다.
- Git 상태 확인, diff, 커밋 및 이력 조회는 반드시 이 저장소를 기준으로 수행한다.
- Unity 프로젝트 루트도 `C:/Unity/MazeParty`이며 Assets 경로는 `C:/Unity/MazeParty/Assets`다.
- 별도의 보조 작업 폴더를 이 게임의 Git 저장소로 간주하지 않는다.

## 노션 기록 원칙

- 연결된 Notion을 기획 변경사항과 Git 커밋 회의록의 기록 장소로 사용한다.
- 기획서는 개인 페이지 `황도연_기획서 모음`에 있는 `재밌을것같은 4인 주사위게임` 페이지다.
  - 기획서 URL: `https://app.notion.com/p/3d3c227fda0380f6a34df09eff208ac0`
- 회의록은 기획서 하위의 `프로젝트 회의록` 데이터베이스에 저장한다.
  - 데이터베이스 URL: `https://app.notion.com/p/f8a0bc24176e4f34804f7a605234ff8f`
  - 데이터 소스: `collection://f17625ac-cf65-4c63-85f7-184f257c270f`
  - 기본 보기: `날짜별 회의록` 캘린더
- Notion을 쓰기 전에 대상 페이지 또는 데이터베이스를 조회해 연결 상태와 접근 권한을 확인한다. 연결이 끊겼거나 쓰기에 실패하면 사용자에게 즉시 알리고 기록을 완료한 것처럼 말하지 않는다.
- 같은 날짜에는 `YYYY-MM-DD 회의록`이라는 하나의 일일 페이지만 사용한다. 새 페이지를 만들기 전에 해당 날짜 페이지를 검색하고, 이미 있으면 그 페이지에 새 변경사항을 누적한다.
- 회의록에는 해당 날짜에 새로 발생한 변경사항만 작성한다. 이전 날짜의 내용이나 변경되지 않은 내용은 반복하지 않는다.
- 날짜는 `Asia/Seoul`의 현지 날짜를 기준으로 한다.

## 기획내용 기록 규칙

- 사용자가 기획 내용을 확정하면 별도의 회의록 작성 요청을 기다리지 말고 기획서와 해당 날짜의 회의록에 즉시 반영한다.
- 확정되어 적용할 기획 변경사항만 회의록과 기획서에 반영한다. 검토 중인 제안과 브레인스토밍은 사용자가 기록을 지시하지 않는 한 반영하지 않는다.
- 확정한 내용을 이후 번복하면 번복 자체를 새로운 기획 변경사항으로 보고 기획서와 번복 날짜의 회의록에 기록한다.
- 기획서에서 변경되는 기존 내용은 삭제하거나 덮어써서 없애지 않는다.
- 변경되는 파트의 기존 내용은 `이전안 · YYYY-MM-DD`처럼 변경 날짜가 드러나는 토글 블록 아래로 옮겨 접힌 상태로 보존하고, 확정된 최신 적용사항을 본문에 표시한다.
- 기획 변경 회의록에는 그 날짜에 확정된 변경사항만 작성한다.

## Git 커밋 회의록 규칙

- Unity MCP 등 MCP를 사용해 프로젝트를 수정했다는 사실만으로는 회의록이나 기획서를 갱신하지 않는다.
- MCP 작업 중간 상태와 개별 MCP 명령 내역은 회의록이나 기획서에 작성하지 않는다.
- Git 커밋이 성공해도 회의록을 자동 작성하지 않는다.
- 사용자가 `회의록을 작성해줘` 또는 같은 의미로 명시적으로 요청했을 때만 성공한 Git 커밋을 회의록에 기록한다.
- 커밋 회의록 요청이 들어오면 현재 대화에서 방금 성공한 커밋을 우선 대상으로 삼는다. 대상 커밋이 여러 개이거나 불명확하면 기록 전에 범위를 확인한다.
- 커밋 기록은 성공한 커밋의 해시, 제목, 본문 및 실제 변경 내용을 확인한 뒤 이를 근거로 작성한다. 추측하거나 아직 커밋되지 않은 변경사항을 섞지 않는다.
- 커밋 회의록의 날짜는 시스템 실행 날짜가 아니라 Git 커밋에 기록된 날짜를 사용한다. 해당 날짜의 일일 회의록 페이지에 변경 요약과 커밋 해시를 누적한다.
- MCP로 만든 변경사항이라도 성공한 커밋에 포함되었다면, 개별 MCP 작업이 아니라 최종 커밋 내용을 기준으로 회의록에 기록한다.
