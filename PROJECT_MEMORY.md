# MazeParty 임시 기획 메모

이 파일은 구현 중이거나 대화 중인 기획의 임시 기준이다. 작업 중에는 이 파일을
갱신하고, 모든 구현이 끝난 뒤 사용자가 회의록 작성을 요청했을 때만 이 내용과
성공한 Git 커밋을 근거로 Notion 기획서 및 일일 회의록을 갱신한다.

## 현재 구현 기준

- PC 온라인 고정 4인, 실행 프로세스당 로컬 플레이어 1명이다.
- 보드 행동은 탑뷰에서 1인칭으로 전환된 뒤 공용 3분 동안 네 플레이어가 동시에 진행한다.
- 아이템 선택은 플레이어별 최대 30초이며 최초 5초 동안 아이템 피해 무적을 적용한다.
- 아이템 선택이 끝나면 주사위 전과 MOVES 소진 후에도 현재 칸 내부에서 자유롭게 이동할 수 있다.
- 주사위는 플레이어 앞에 놓이는 서버 권한 D12 월드 오브젝트이며 소유자만 굴리거나 밀 수 있다.
- 주사위를 좌클릭으로 밀면 현재 칸 안에서 위치를 가볍게 조정한다. 우클릭으로 굴림을 시작하면 서버가 1~12를 균등하게 선추첨하고 물리 임펄스와 회전력을 적용한다. 0.65초 물리 텀블 뒤 0.35초 동안 선택된 숫자 면이 위로 오도록 착지를 보정하며 현재 칸 경계를 넘지 않는다.
- 굴림 연출은 총 1초이고 착지 완료 시 이동 수를 확정한다. 결과는 모든 플레이어가 주사위 윗면과 월드 숫자로 확인하며 1초 공개 후 주사위를 숨긴다. 행동 제한시간 직전에 서버가 승인한 굴림은 착지와 결과 반영까지 기다린 뒤 다음 단계로 진행하며, HUD에는 굴림 중 상태와 남은 결과 공개 시간을 이어서 표시한다.
- D12는 플레이어별 빨강·파랑·초록·노랑 색을 적용하고 검은 숫자는 유지한다. 원본 Ignore 패키지를 런타임에서 직접 참조하지 않고 필요한 FBX와 512px 텍스처만 추적되는 `Assets/MazeParty/Art/Dice/D12`로 복사해 사용한다.
- 2026-09-10 기준 전체 EditMode 테스트 224개가 모두 통과했다. 실제 BoardFlow Testbed Play Mode에서 D12 굴림 후 확정 숫자와 상단 면이 일치하고 관련 Console 오류·경고가 없음을 확인했다.
- 다른 플레이어의 HUD에는 정확한 주사위 숫자를 표시하지 않는다.
- 플레이어별 경계 벽 4개, 총 16개를 재사용한다. 파란 경계는 통과 가능하고 검은 경계는 물리적으로 막는다.
- 1턴에는 열쇠 상점이 없으며 2턴 탑뷰에서 플레이어가 없는 일반 칸 하나에 유일한 상점을 생성한다.
- 게임 중 비호스트 연결이 끊기면 최대 60초 동안 전체 시뮬레이션을 정지하고, 복귀하면 저장 상태로 즉시 재개한다.

## 구현 완료: 플레이어 스탯과 칸 효과

- 플레이어 스탯은 최대 체력, 현재 체력, 보유 열쇠 수, 보유 골드 수로 구성한다.
- 초기 스탯은 최대 체력 100, 현재 체력 100, 열쇠 0, 골드 10이다.
- 플레이어 상태 UI는 체력바, 열쇠, 골드, 행동 아이콘의 네 요소로 구성하며 실시간으로 반영한다.
- 행동 아이콘 상태는 주사위 굴리기, 이동 중, 도착, 격투의 네 가지다.
- 행동 아이콘은 아이템 선택부터 주사위 결과 확인까지 Dice, 이동 수가 남은 동안 Moving, 이동 수 소진 후 칸 내부 활동과 격투 대기 동안 Arrived, 최종 칸 격투 동안 Fighting을 표시한다. 탑뷰 칸 효과, 미니게임, 재접속 대기 등 네 상태 밖에서는 숨긴다.
- 상태 HUD는 화면 왼쪽에 네 플레이어 패널을 세로로 배치한다. 각 패널에는 현재/최대 체력이 표시되는 체력바, 열쇠, 골드, 행동 아이콘을 두며 로컬 플레이어 패널을 강조한다.
- 체력, 열쇠, 골드는 서버 권한 상태로 변경하고 모든 클라이언트 UI에 즉시 동기화한다.
- 골드는 0 미만이 될 수 없으며, 손실량보다 보유 골드가 적으면 보유한 만큼만 잃는다.
- 미리 디자인된 보드 칸 위에 경기 시작 시 칸 효과를 무작위로 배정한다.
- 첫 구현 대상은 골드 획득 효과와 골드 손실 효과다.
- 골드 획득/손실 기본값은 +3/-3이며 후반 배율은 최초 구현에 적용하지 않는다.
- 테스트용 배정 비율은 골드 획득 5 : 골드 손실 3 : 아이템 획득 1 : 회복 1이다. 서버가 경기 시작 시 한 번만 배정하고 15턴 동안 고정한다.
- 플레이어는 이동과 최종 칸 격투가 모두 끝난 뒤 도착 칸의 효과를 받는다.
- 칸 효과 처리와 연출은 탑뷰에서 진행한다.
- 칸 효과는 열쇠 상점과 겹칠 수 있지만 리스폰 칸에는 배정하지 않는다.
- 프로토타입 시각 표시는 골드 획득 칸을 파란색, 골드 손실 칸을 빨간색, 아이템 획득 칸을 보라색, 회복 칸을 초록색으로 구분한다.
- 플레이어 시작 위치는 칸의 종류나 효과가 아니라 단순한 초기 배치 정보다. 해당 칸은 다른 일반 칸과 동일하게 효과 배정 대상이 되며 최초 스폰만으로는 효과를 발동하지 않는다.
- 최종 칸 격투 후 같은 칸에 여러 플레이어가 남는 경우는 없다. 효과는 4초의 탑뷰 정산 단계에서 격투 종료 후 플레이어 슬롯 1번부터 4번 순서로 1초 간격으로 정산한다.
- 격투 시스템을 구현하기 전에는 명시적인 CombatResolved 연결 지점을 두고 자동 완료 처리한다. 추후 실제 격투 종료 신호가 같은 연결 지점을 호출한다.
- 열쇠 상점 가격은 열쇠 1개당 골드 20으로 정한다.
- 수치 레퍼런스: Mario Party의 일반 Blue/Red Space는 기본 +3/-3 코인이며, Pummel Party의 기본 Key Space는 패치 1.0.9 기준 +5 Keys다. Pummel Party 기본 보드에는 이에 대응하는 일반 손실 칸이 없다.

### 확정 전 항목

- 없음

## 구현 완료: 상점, 추가 칸 효과와 현재 순위

- 열쇠 상점은 월드 오브젝트이며, 우클릭 요청이 서버에 먼저 승인된 플레이어가 골드 20을 지불하고 열쇠 1개를 획득한다.
- 열쇠 구매가 승인되면 공용 행동 타이머와 보드 시뮬레이션을 정지하고, 열쇠 상점 이동 및 새 위치 확인을 위한 전원 탑뷰 연출을 진행한 뒤 정지 직전 상태로 복귀한다.
- 열쇠 상점 이동 탑뷰는 5초 동안 유지한다. 새 상점은 탑뷰 종료 즉시 활성화되며 같은 턴에도 다시 구매할 수 있다.
- 열쇠 상점의 새 위치는 이전 위치, 리스폰 칸, 모든 플레이어 위치, 두 아이템 상점 위치를 제외한다. 골드가 부족하면 구매·정지·이동이 모두 발생하지 않는다.
- 아이템 획득 칸은 탑뷰 칸 효과 정산 시 무작위 아이템 하나를 지급한다. 우선 현재 프로토타입 아이템으로 테스트하며 아이템 다양화는 후속 TODO다.
- 아이템 획득 시 인벤토리가 가득 찼으면 대체 보상 없이 획득에 실패한다.
- 아이템 상점 월드 오브젝트는 보드의 무작위 칸 두 곳에 생성하며 열쇠 상점과 같은 칸에는 놓지 않는다.
- 두 아이템 상점은 1턴 탑뷰부터 등장하고 각각 독립된 판매 목록을 가진다. 서로, 리스폰 칸, 플레이어 위치와 겹치지 않으며 칸 효과와는 겹칠 수 있다.
- 아이템 상점은 우클릭으로 열고, 상점 UI에는 무작위 판매 아이템 5개를 표시한다.
- 각 판매 항목은 재고 1개만 가지며, 먼저 구매된 항목은 다른 플레이어가 구매할 수 없다. 남은 항목은 계속 구매할 수 있다.
- 한 플레이어도 인벤토리와 골드가 허용하는 한 서로 다른 판매 항목을 여러 개 구매할 수 있다.
- 상점 아이템은 클릭 즉시 구매를 요청한다. 인벤토리 3칸이 가득 찼다면 UI는 열리지만 구매와 골드 차감은 거부한다.
- 상점 UI의 아이템 표현과 마우스 호버 정보는 개인 아이템 선택 UI의 표시 컴포넌트를 재사용한다.
- 상점 UI가 열린 동안 공용 타이머는 계속 흐르고 피격·밀림은 유지한다. 해당 플레이어의 자발적 이동과 공격은 잠그며 상호작용 범위를 벗어나면 UI를 자동으로 닫는다.
- 아이템 상점은 5개 재고가 모두 판매되거나 배치 후 5턴이 지나면 위치를 옮기고 판매 목록과 재고를 새로 만든다.
- 품절된 상점은 남은 턴 동안 `SOLD OUT` 상태로 유지하고 다음 탑뷰에서 이동·재입고한다. 1턴에 배치된 미품절 상점은 6턴 탑뷰에서 이동·재입고한다.
- 현재 테스트 카탈로그와 가격은 Pulse Blaster 7골드, Push Mine 5골드, Med Kit 5골드다. 5개 판매 항목에는 동일 종류가 중복될 수 있지만 각 항목 재고는 별개다.
- 회복 칸은 탑뷰 칸 효과 정산 시 체력을 50 회복한다.
- 회복은 최대 체력을 넘지 않으며, 이미 최대 체력이면 대체 보상을 지급하지 않는다.
- 플레이어 정보 HUD에 현재 순위를 표시한다. 공동 순위는 높은 순위를 함께 사용하고 그 인원수만큼 다음 순위를 건너뛰는 경쟁 순위 방식으로 표시한다. 예: 1위, 1위, 3위, 4위.
- 기획서의 순위 비교 기준은 열쇠 수, 보유 골드, 미니게임 1위 횟수 순이다.
- 순위 계산을 위해 서버 동기화되는 미니게임 1위 횟수 상태를 추가했으며 현재 초기값은 0이다.
- 플레이어 인벤토리는 기존 테스트용 3개 아이템 선지급을 제거하고 3슬롯 모두 빈 상태로 시작한다.

### 확정 전 항목

- 없음

## 구현 완료: 보드 UI 프리팹화

- 보드 런타임 Canvas의 공용 원본은 `Assets/MazeParty/UI/Prefabs/BoardCanvas.prefab`으로 관리한다.
- 온라인 Board 씬과 로컬 Board Flow Testbed 씬은 같은 공용 프리팹을 인스턴스화한다. 테스트베드 전용 시뮬레이터 구성은 프리팹 오버라이드로 유지한다.
- 스프라이트, 머티리얼, 폰트, 색상, 버튼 전환, RectTransform 배치와 장식 자식 오브젝트는 프리팹에서 교체하거나 수정할 수 있다.
- 씬 재구축 명령은 기존 프리팹을 덮어쓰지 않는다. 프리팹이 없는 최초 1회에만 기본 UI를 생성한다.
- 런타임 바인딩에 쓰는 오브젝트 이름과 Text/Image/Button/BoardItemChoiceButton 컴포넌트는 보호 계약으로 검증한다. 필수 앵커가 누락되면 씬 재구축을 중단하고 오류를 표시한다.
- Unity 메뉴 `MazeParty > UI > Open Board Canvas Prefab`으로 공용 UI 프리팹을 바로 열 수 있다.
- 한글 폰트가 추가되기 전까지 화면 표기는 영어를 유지한다.

## 구현 완료: 최종 칸 격투

- 모든 플레이어의 이동 및 상승 정리 후, 칸 효과 정산 전에 최종 칸 격투를 진행한다.
- 같은 칸에 2명 이상 도착한 경우만 격투를 생성한다. 최초 격투 큐는 칸이 격투 조건을 충족한 시각 순이며, 동시에 성립하면 현재 최하위 플레이어가 포함된 격투를 우선한다. 이후에도 동률이면 칸 좌표 순으로 고정한다.
- 한 번에 한 칸의 격투만 진행하고, 비참가자와 탈락자는 현재 격투 칸을 보여주는 관전 카메라로 전환한다.
- 참가자는 도착한 위치를 유지한 채 1인칭 시점으로 시작한다. 칸의 네 경계를 모두 검은 벽으로 막고 WASD 이동, 마우스 시점, 좌클릭 주먹 공격만 허용한다. 주사위, 아이템, 상점 및 우클릭 입력은 잠근다.
- 전투 임시 체력은 매 격투 시작 시 100이며 일반 체력과 분리한다. 주먹 피해는 5, 사거리는 2m, 공격 간격은 0.65초, 전투 제한 시간은 1분이다.
- 주먹은 짧은 전방 범위에서 가장 가까운 한 명만 맞히고 약한 물리 밀림을 적용한다. 기절, 다운, 연속 경직은 적용하지 않는다.
- 임시 체력이 0이 되면 즉시 이동, 공격 및 충돌을 비활성화하고 관전 상태로 전환한다. 골드나 일반 체력은 변경하지 않는다.
- 전투 종료 순위는 생존자를 먼저 두고, 생존자끼리는 남은 임시 체력 순으로 정한다. 탈락자는 더 늦게 탈락한 순으로 정한다. 이후 동점은 도착 시각, 현재 종합 순위가 더 낮은 플레이어, 플레이어 슬롯 순으로 해소한다.
- 전투 순위별 후퇴 거리는 `순위 - 1`칸이다. 이동 이력을 역순으로 따라가며, 이력이 부족하면 현재 칸으로 들어오는 경로 중 좌표 기준의 결정적 경로를 사용한다.
- 후퇴 결과 다시 같은 칸에 2명 이상이 모이면 연쇄 격투를 큐 뒤에 추가한다. 일반 격투와 연쇄 격투가 모두 끝난 후 각 플레이어의 최종 칸 효과를 한 번씩 정산한다.
- 격투 참가자는 다음 행동 단계 시작부터 30초 동안 개인 아이템 피해 방어를 받는다. 재접속 전체 정지나 열쇠 상점 확인 정지 중에는 이 시간도 감소하지 않는다.
- 이 개인 방어는 아이템에 의한 일반 체력 피해만 막는다. 공격 적중, 아이템 소모, 밀림, 둔화와 격투 임시 체력 피해는 막지 않는다. 기존 행동 시작 5초 방어와 겹치면 더 긴 남은 시간을 HUD에 표시한다.
- 에디터 테스트베드에는 P1/P2 격투 배치 도구, 전투 임시 체력 HUD, 1분 타이머, 좌클릭 공격, P1 피격 시뮬레이션과 관전 전환을 둔다.

## 미구현 TODO

- 제한 시간 종료 시 남은 이동 수 강제 정산
- 15턴 종료, 결과 및 재화 이동
- 최종 장막 VFX, 엄폐물과 레벨 디자인
- 고급 타격 VFX와 전투 사운드(캐릭터 피격 흔들림은 구현, 카메라에는 전달하지 않음)
- 아이템 종류와 가격 다양화
- Minefield 외 추가 미니게임 선택과 순환

## 구현 완료: 이동과 발걸음 소리

- 보드의 1인칭 이동 중 `Left Ctrl`을 누른 채 WASD로 이동하면 저소음 걷기 상태가 된다.
- 기본 이동 속도는 5m/s이며 저소음 걷기는 55%인 2.75m/s로 이동한다.
- 기본 발걸음의 월드 가청 반경은 24m이고 저소음 걷기는 6m다.
- 발걸음 간격은 실제 수평 이동 거리 기준이며 기본 이동은 1.8m, 저소음 걷기는 1.35m마다 발생한다.
- 저소음 걷기는 보드 행동 단계와 최종 칸 격투 모두에서 사용할 수 있다.
- 발걸음 사운드 클립, 캐릭터 이동 애니메이션과 모션 표현은 후속 TODO로 남긴다. 이번 구현은 이동 상태, 네트워크 발걸음 이벤트와 추후 클립을 연결할 재생 지점을 우선 구축한다.
- 기존 기획 기준상 소리 범위는 칸 수가 아닌 월드 반경으로 판정하고 벽에 의한 차폐는 적용하지 않는다. 기본 발걸음의 초기 목표 반경은 약 3칸 크기다.

## Notion 반영 대기

- 플레이어 스탯·4인 상태 HUD·칸 효과·상점·순위·빈 인벤토리 시작·보드 UI 프리팹화·최종 칸 격투·이동과 발걸음 구현 내용은 아직 커밋 및 회의록 작성 요청 전이므로 이 파일에만 보관한다.
- 2026-09-08 회의록 및 커밋 `0eda9bff07d665352a39fad07d8e3076854aed04`까지 Notion 반영 완료

## 현재 구현 기준: 플레이어 외형, 애니메이션과 커스터마이징

- 이 구현이 처음 진행된 브랜치는 `dev/loby`였고, 현재 후속 작업 브랜치는
  `dev/minigame`이다.
- 캐릭터 도안은 서 있는 정면/측면과 앉은 정면/측면이며 작은 원은 손이다.
- 리깅 없는 모듈형 3D 파츠로 구현한다. 기존 네트워크·이동·충돌 루트와
  `VisualRoot`를 분리하고, 머리·몸·양손·얼굴·모자·옷 슬롯을 고정 앵커로 둔다.
- 앉기는 `Left Ctrl`을 누르는 동안 유지되는 저소음 걷기 상태와 통합한다.
  보드의 1인칭 행동 단계와 최종 칸 격투에서 앉은 채 이동 및 회전할 수 있다.
- 첫 애니메이션 범위는 Idle, 일반 이동, 저소음 이동, 앉기 전환/유지/해제,
  펀치, 피격, 탈락이다. 점프는 구현하지 않는다.
- 눈과 입은 서로 독립된 ID로 동기화한다. 현재 기본 눈/입만 제공하고 감정표현과
  추가 얼굴 조합은 후속 TODO로 둔다.
- 플레이어 몸 색은 빨강, 주황, 노랑, 초록, 파랑, 남색, 보라, 검정의
  고정 8색 팔레트에서 선택한다. 같은 방의 플레이어끼리는 같은 색을 사용할 수 없다.
- 플레이어 머리 위에 닉네임을 표시한다.
- 모자 교체 검증용 테스트 모자 리소스 하나를 함께 만든다.
- 커스터마이징 선택은 로컬에 저장하고 온라인에서 다른 플레이어에게 ID와 색상으로
  동기화하는 방향을 기본으로 한다.
- 렌더링은 Pummel Party 같은 둥글고 부드러운 3D 클레이 캐릭터를 목표로 한다.
- 클레이 표현은 굵은 검은 외곽선 대신 URP의 부드러운 조명과 무광 재질을
  사용하고, 눈과 입 같은 얼굴 요소에만 검은색을 사용한다.
- 앉을 때 시각 파츠, 총기 피격 부위, CharacterController와 1인칭 카메라 높이를
  함께 낮춘다. 키를 놓았을 때 머리 위 공간이 부족하면 앉은 상태를 유지한다.
- 1인칭 로컬 플레이어는 월드 머리·얼굴·모자·닉네임을 숨기고 소유자 전용
  1인칭 손을 표시한다. 탑뷰와 원격 플레이어에는 전체 월드 모델과 닉네임을 표시한다.
- 색/무늬 옷은 머티리얼 교체로 서기/앉기에서 공용 사용한다. 실루엣을 바꾸는
  옷은 같은 원점과 규격의 서기/앉기 모델 한 쌍을 요구한다.
- 총기 공격은 몸통 100%, 머리 130%, 손 70%의 부위별 피해 배율을 적용한다.
  총기가 아닌 근접·환경·범위 피해에는 이 배율을 적용하지 않는다.
- 온라인 Pulse Blaster 실제 발사를 서버 권한 히트스캔으로 구현한다. 사거리 40m,
  몸통 기본 피해 20이며 머리 26, 손 14로 계산한다. 서버가 발사 원점과 방향을
  검증하고 기존 행동 단계 아이템 방어 규칙은 그대로 적용한다.
- 방 입장 후 로비에서 8개 몸 색 버튼과 `None/Test Hat`을 바꿀 수 있다. 입장 시
  서버가 사용 중이지 않은 첫 색을 배정하며, 색 선택은 서버가 중복 여부를 확인한 뒤
  승인한다. 동시에 같은 색을 고르면 서버에 먼저 도착한 요청만 승인하고 나머지는
  기존 색을 유지한다. 승인된 값만 로컬 프로필에 저장하고 전원에게 동기화한다.
- 한국어 닉네임 입력·로컬 저장·네트워크 동기화를 지원하며 최대 16자/UTF-8 60바이트다.
  실제 글리프 표시는 `Assets/Resources/MazeParty/Fonts/PlayerNameFont.ttf` 또는
  같은 경로의 OTF에 사용권이 확보된 한글 폰트를 추가해야 완성된다. 폰트가 없으면
  Unity 기본 폰트로 폴백하므로 한국어가 네모로 보일 수 있다.
- 프로토타입 외형·애니메이션은 Unity 기본 프리미티브와 URP Lit 런타임 재질로
  구성한다. 실제 아트 교체 규격은
  `Assets/MazeParty/Art/Player/PLAYER_ASSET_REQUIREMENTS.md`를 기준으로 한다.
- 2026-09-09 `dev/Player` 구현 시점의 관련 EditMode 테스트 127개가 모두 통과했다.
  이후 `dev/loby` 회귀 테스트를 추가했으며 테스트 어셈블리 컴파일은 통과했다.
  Unity Test Runner 전체 재실행은 에디터 MCP 명령 대기 문제 해소 후 확인한다.

## 현재 구현 기준: 일반 펀치, 부위 흔들림과 아이템 표시

- 보드 행동 단계에서 아이템을 선택하지 않은 플레이어는 좌클릭으로 일반 펀치를
  사용한다. 서버가 0.5초 간격, 1.65m 사거리, 0.3m 반경을 검증하며 공격할 때마다
  왼손과 오른손 모션을 번갈아 모든 클라이언트에 표시한다.
- 일반 펀치는 다른 플레이어의 일반 체력과 격투 임시 체력을 감소시키지 않으며
  밀림도 적용하지 않는다. 맞은 시각 파츠만 흔들린 뒤 원래 자세로 복귀한다.
- 몸 피격은 몸통과 외투 앵커가 오뚝이처럼 좌우로 흔들리고, 머리 피격은 머리와
  얼굴·모자 파츠가 몸 위에서 더 과장되게 좌우로 흔들린다. 손 피격은 양손 파츠에
  짧은 흔들림을 적용한다.
- 피격 흔들림은 `VisualRoot`의 렌더 파츠에만 적용한다. 카메라 피벗, 이동 루트,
  CharacterController와 서버 판정용 hitbox에는 영향을 주지 않는다.
- 최종 칸 격투의 좌클릭은 같은 좌우 교대 펀치 모션을 사용하면서 기존 5 임시
  체력 피해와 밀림을 유지한다.
- 총기 피해 요청에 적중 부위를 포함해 머리·몸·손에 대응하는 흔들림을 재생한다.
  기존 몸 100%, 머리 130%, 손 70% 피해 규칙은 유지한다.
- 아이템 사용 시 0.55초 동안 1인칭/월드 양손을 숨기고 선택한 아이템 모델만
  고정된 아이템 사용 앵커에 표시한다. 현재 Pulse Blaster, Push Mine, Med Kit는
  Unity 프리미티브 임시 모델이며 실제 모델 요구사항은 플레이어 에셋 문서에 기록한다.
- 좌클릭을 누르고 있으면 보드 일반 펀치는 0.5초, 최종 칸 격투 펀치는 기존 0.65초
  간격으로 반복되며, 서버가 승인한 공격마다 양손을 계속 번갈아 사용한다. 아이템은 홀드로
  여러 슬롯이 연속 소모되지 않도록 최초 클릭에만 사용한다.
- 월드 주사위를 굴리기 위해 밀거나 위치를 가볍게 조정하는 요청이 서버에서 승인되면
  같은 교대 펀치 모션을 재생한다. 주사위 위치 조정 홀드는 0.5초 서버 제한을 적용한다.

## 현재 구현 기준: dev/loby 빌드 멀티 수정

- 장막 통과 중 캐릭터 중심이 다음 칸에 먼저 들어가더라도 논리 칸을 조기 변경하지 않는다.
  캐릭터 캡슐 전체가 장막을 통과해 게이트 커밋이 성립할 때만 다음 칸으로 변경하고
  남은 이동 횟수를 1 감소시킨다.
- 플레이어 전용 파랑/검정 경계 벽은 프리팹에 저장된 URP Lit 머티리얼을 사용한다.
  빌드 셰이더 스트리핑 영향을 받지 않도록 같은 머티리얼을 Resources에도 보관하고,
  직렬화 참조가 없을 때 이 머티리얼로 폴백한다.
- 온라인 대기방은 고정 카메라 안에 들어오는 12m x 8m 소형 방이다. 4개 슬롯은 서로
  2m 이상 떨어진 위치에 생성되며, 보드 씬이 로드되기 전까지 WASD로 방 안을 이동한다.
- 대기방 이동 방향이 마지막 바라보는 방향이 된다. 좌클릭 펀치는 그 방향으로 공격하며,
  일반 체력과 탈락 상태에는 영향을 주지 않고 피격 모션과 약한 밀림만 적용한다.
- 준비 완료 상태에서도 대기방 이동과 공격을 유지한다.
- 로비 몸 색은 빨강/주황/노랑/초록/파랑/남색/보라/검정 8개 버튼으로만 선택한다.
  다른 플레이어가 사용 중인 색 버튼은 비활성화되고 퇴장 시 다시 사용할 수 있다.

## 현재 구현 기준: 첫 미니게임 Machine Party - Minefield

- 첫 Minefield 구현과 후속 개발자 도구 작업 기준 브랜치는 현재 `dev/minigame`이다.
- 보드 칸 효과 정산이 끝나면 탑뷰 상태에서 Minefield 룰 이미지와 READY UI를 표시한다.
  네 플레이어가 모두 READY를 누르고 NGO additive scene 동기화가 끝나면 Minefield로
  전환한다. 룰 이미지는 추후 제작하며 현재는 교체 가능한 이미지 앵커와 안내 문구를 쓴다.
- 플레이도 탑뷰이며 서버가 별도 논리 러너 네 명의 위치·상태·입력을 권한 있게 계산한다.
- 한 경기는 3라운드다. 매 라운드는 3초 카운트다운, 40초 플레이, 4초 결과 표시로
  진행하고 새 지뢰 배치를 생성한다.
- 플레이어가 멈춘 상태에서 우클릭하면 0.75초 동안 반경 6m 이내의 지뢰를 탐지한다.
  머리 위 사이렌은 가장 가까운 지뢰가 6m 안에 있을 때 켜지고 가까울수록 진해진다.
- 첫 지뢰 피격은 유혈 없이 몸통과 옷을 숨기고 양손을 머리 옆에 둔다. 이동 속도는
  2.75m/s이며 두 번째 지뢰 피격 시 탈락한다.
- 후방 분쇄기는 현재 큐브 플레이스홀더다. 도착자는 도착 순, 미도착자는 먼저 탈락한
  순으로 순위를 정하고 라운드마다 3/2/1/0점을 지급한다.
- 3라운드 합산 포인트로 최종 순위를 정하고 현재 골드 지급도 3/2/1/0을 사용한다.
  최종 1위의 미니게임 승수를 1 올린다.
- Minefield 씬은 빌드 설정에 포함하고 네트워크 additive load로 이동한다. 세션 종료 시
  Board와 함께 로컬에서 해제한다.

## 현재 구현 기준: 빌드 렌더링·장막·프레임 보정

- 상점과 런타임 캐릭터 프리미티브는 Resources의 URP Lit 머티리얼을 기반으로 생성해
  빌드 셰이더 스트리핑으로 마젠타가 되는 문제를 방지한다.
- 3D `TextMesh`에는 `ZTest LEqual`인 빌드 포함 셰이더를 적용해 벽과 오브젝트 뒤의
  글자가 깊이 검사를 무시하고 비쳐 보이지 않게 한다. 화면 UI는 이 변경 대상이 아니다.
- Standalone 실행 시 VSync를 끄고 `Application.targetFrameRate`를 60으로 설정한다.
- 플레이어 전용 장막은 로비와 대기실에서 항상 숨긴다. 보드에 들어간 뒤 현재 칸에서
  입구 또는 출구로 물리적으로 연결된 길 방향에만 장막 오브젝트를 활성화한다. 현재
  이동 가능한 출구는 파란색으로 통과시키고, 들어오기만 하는 일방통행이나 이동 횟수가
  없어 갈 수 없는 연결부는 검정색 충돌 벽으로 남긴다. 연결 자체가 없는 면의 추가
  장막은 비활성화하고 기존 보드 벽을 사용한다.

## 현재 구현 기준: 로비 종료 버튼

- OnlineBootstrap 로비 창 하단에 `Quit Game` 버튼을 항상 제공하되, 보드 게임 진행
  중에는 숨긴다. 연결 전 메인 로비와 방에 입장한 대기 상태에서는 표시한다.
- 버튼을 누르면 Standalone 빌드는 애플리케이션을 종료하고, Unity Editor Play Mode에서는
  플레이 모드를 종료해 같은 동작을 바로 테스트할 수 있다.

## 현재 구현 기준: 카메라 및 미니게임 UI

- 보드의 전체화면·1인칭·전투·미니게임 카메라 전환은 멀미를 유발하는 이동 보간 없이
  즉시 컷으로 전환한다.
- Minefield는 각 클라이언트가 자신의 플레이어를 중심으로 따라가는 직교 탑뷰를 사용한다.
  완전 수직에서 10도 기울인 시점이며, 다른 플레이어도 주변 시야 안에서 볼 수 있다.
- Board 씬이 로드되면 로비의 방 정보·플레이어 목록 Canvas를 숨긴다. Minefield 실제
  플레이 중에는 Board Canvas도 렌더링과 입력을 모두 끄고 Minefield 전용 HUD만 표시한다.
  규칙 확인/준비 단계와 최종 결과 단계에서는 해당 Board UI를 다시 사용한다.

## 현재 구현 기준: 보드 도착 유예·탑뷰 강조·시간초과 이동

- 1인칭 시선의 yaw/pitch는 서버가 동기화한 몸 방향과 분리해 로컬 플레이어가 마지막으로
  바라본 각도를 유지한다. 서버 물리 갱신이나 NetworkTransform 보정 뒤에도 눈 피벗을
  다시 계산한 후 카메라가 그 최종 자세를 사용한다.
- 네 번째 플레이어가 칸에 도착하면 즉시 탑뷰로 바꾸지 않고 서버 동기화된 3초 유예를
  둔다. 현재는 디버깅 확인을 위해 Board HUD에 `TOP VIEW` 소수점 카운트다운과 안내를
  표시하며, 추후 연출 확정 시 이 디버그 문구만 제거한다.
- 보드 탑뷰에서는 로컬 플레이어 발밑과 현재 활성화된 열쇠 상점·아이템 상점 둘레에
  빌드 안전한 흰색 라인 하이라이트를 표시한다.
- 180초 행동 시간이 끝날 때 도착하지 못한 플레이어는 서버가 현재 칸의 유효한 출구 중
  하나를 결정해 다음 칸으로 1칸 강제 이동시킨 뒤 정산한다.

## 현재 구현 기준: Minefield 배치·재접속 표시 보정

- Minefield 가로 격자는 3칸이며 매 라운드 지뢰 수는 20개다. 시작·도착 쪽 한 줄 깊이를
  안전지대로 제외하고, 나머지 연속 좌표 범위 안에서 서버 시드로 위치를 자동 생성한다.
  지뢰는 칸 중앙이나 칸당 1개 제한을 사용하지 않으며 최소 간격만 유지한다.
- 사이렌 렌즈·베이스·탐지 지뢰와 소나 라인은 Resources에 포함된 URP Lit 머티리얼을
  사용해 Standalone 빌드에서 마젠타가 되지 않도록 한다.
- 일반 보드에서 클라이언트가 끊기면 기존 Board 재접속 오버레이가 60초 서버 타이머를
  표시한다. Minefield 중에는 Board UI를 계속 숨기되 Minefield HUD 자체가 같은 서버
  재접속 타이머를 표시한다. 호스트가 종료된 경우 권한 서버가 사라지므로 현재 고정 호스트
  구조에서는 재접속 유예 대신 세션 종료로 처리하며 호스트 마이그레이션은 별도 범위다.
- 2026-09-10 기준 관련 Gameplay/Multiplayer EditMode 테스트 172개가 모두 통과했다.
  Windows 64-bit Development 빌드도 오류 0개로 성공했으며, 남은 경고 2개는 사용하지 않는
  `Hidden/Core/DebugOccluder` 계열 디버그 셰이더 스트리핑 경고다.

## 현재 구현 기준: 에디터 미니게임 솔로 테스트 도구

- Unity 메뉴 `MazeParty > Developer > Minigame Solo Tester`에서 등록된 미니게임을
  선택해 1인 로컬 테스트를 시작한다. 현재 카탈로그에는 Minefield와 WrongWay가
  등록되어 있으며 각 미니게임의 빠른 실행 메뉴도 제공한다.
- 실행 시 제작용 Minefield 씬을 Play Mode 시작 씬으로 임시 지정하고 런타임 하네스를
  주입한다. 원래 열려 있던 에디터 씬과 기존 Play Mode 시작 씬 설정은 종료 후 복구하며,
  제작 씬 에셋과 Build Settings는 수정하지 않는다.
- 방 생성, UGS 로그인, NGO 세션 및 다른 플레이어 없이 제작용 경기장·입력·이동·지뢰·
  소나·분쇄기·카메라 컴포넌트를 재사용한다. 제작 코드와 같은 시드 기반 20개 지뢰 배치와
  3초 카운트다운, 40초 플레이, 4초 결과 표시를 3라운드 동안 실행한다.
- 조작은 WASD 이동, 정지 중 우클릭 소나, `R` 현재 라운드 재시작, `N` 다음 시드,
  `Esc` 테스트 종료다. 개발자 HUD에서 단계·남은 시간·상태·지뢰 수·클리어 수·시드를
  확인하고 버튼으로도 재시작, 다음 시드, 종료를 수행할 수 있다.
- 이 도구는 로컬 조작·위험물·연출·라운드 타이밍·결정적 배치를 빠르게 검증하는 용도다.
  RPC, 소유권, 서버 권한과 4인 동기화는 기존 멀티플레이 테스트 절차로 별도 검증한다.
- WrongWay 솔로 도구는 제작 씬에서 50계단, 방향 HUD, 선두 추적 카메라와 넘어짐을
  표시한다. 조작은 방향 입력 `WASD`, `R` 현재 라운드 재시작, `N` 다음 시드,
  `Esc` 테스트 종료다.
- 2026-09-10 기준 전체 EditMode 테스트 224개가 모두 통과했다. 실제 WrongWay Solo
  Play Mode에서 방향 입력, 10계단 진행, 오답 후 0.5초 잠금과 현재 문제 유지,
  50계단 도착 후 라운드 결과 전환을 확인했다.

## 현재 구현 기준: 두 번째 미니게임 WrongWay 변형과 미니게임 큐

- 화면에 제시되는 네 방향을 `WASD`로 빠르게 맞혀 계단을 오르는 4인 경쟁 게임이다.
- 총 50개 계단을 사용하며 한 명이 50번째 계단에 먼저 도착하면 경기를 종료한다.
  나머지 플레이어는 종료 순간 올라온 계단 수가 많은 순서로 순위를 정한다.
- 한 경기는 2라운드다. 같은 라운드의 네 플레이어는 서버가 만든 같은 방향열을 각자
  속도로 진행하고, 2라운드에서는 새 방향열을 사용한다. 각 라운드 제한시간은 60초다.
- 오답 입력 시 계단을 잃지 않고 현재 문제가 유지된다. 캐릭터가 넘어지는 동안 0.5초간
  입력을 받지 않으며 입력 버퍼도 두지 않는다. 분쇄기나 탈락을 유발하는 별도 죽음
  장치는 사용하지 않는다.
- 라운드 종료 시 같은 계단에 있는 플레이어끼리는 해당 계단에 먼저 도착한 서버 순서로
  고유 순위를 정한다. 최종 순위에는 기존처럼 3/2/1/0 골드와 1위 승수 1회를 적용한다.
- 라운드별 순위에 3/2/1/0 경기점수를 주고 2라운드 합산으로 최종 순위를 정한다.
  합산 동점은 두 라운드 총 계단 수, 2라운드 순위, 서버 처리 순서 순으로 해소한다.
- 게임 시작 시 전체 턴 수와 같은 길이의 미니게임 큐를 한 번 만든다. 현재 기본 게임은
  15턴이므로 15개 슬롯이며, 턴 수가 바뀌면 큐 길이도 같이 바뀐다. 큐에는 전체
  미니게임 중 서로 다른 게임을 최대 슬롯 수만큼 저장하고, 구현된 게임이 부족한 슬롯은
  미니게임 없이 스킵한다.
- 현재는 Minefield와 WrongWay를 한 번씩, 나머지 13개를 스킵 슬롯으로 넣고 15개 전체를
  게임 시작 시 한 번 섞는다. 실제 게임도 15턴에 종료하며, 향후 총 턴 수가 바뀌면
  큐 길이와 종료 턴이 같은 설정을 참조한다.
- 미니게임 공개 화면에서는 남은 큐를 블록 탑처럼 쌓아 미래 항목을 `???`로 숨기고,
  현재 항목을 하나씩 미니게임 제목으로 공개한다. 큐를 미리 저장해 같은 항목을 재시작할
  때 미니게임 종류가 다시 추첨되지 않게 한다.
- 블록 탑은 미니게임 소개 전에 기존 준비 화면 안 UI로 표시한다. 남은 블록 전체를
  `???`로 보이고, 큐의 맨 앞인 최상단 블록만 제목 또는 `SKIP`으로 공개한 뒤 소비한다.
  스킵은 별도 READY나 씬 로드 없이 짧게 공개하고 다음 턴으로 진행한다.
- 전체 큐는 호스트의 `Application.persistentDataPath` 아래에 논리 매치 ID별 JSON으로
  저장하고, 활성 논리 매치 ID는 인증 프로필별 PlayerPrefs에 둔다. 따라서 같은 경기를
  재시작하거나 호스트 프로세스를 종료한 뒤 새로 실행해도 큐 순서는 다시 추첨되지 않는다.
  호스트가 명시적으로 방을 나가거나 15턴을 완료하면 활성 큐를 정리해 다음 새 경기는
  새 큐를 만든다. 미래 큐 전체는 호스트에만 두고 클라이언트에는 현재 공개 항목과 남은
  블록 수만 복제한다.
- WrongWay 제작 씬은 네 개의 평행 레인에 각각 50계단을 배치하고, 분쇄기·죽음 장치와
  별도 출력 Camera/AudioListener 없이 Board 출력 카메라와 전용 Cinemachine 시점을
  사용한다. Windows 64-bit 비개발 빌드는 오류 0개로 성공했다.
