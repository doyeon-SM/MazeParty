# MazeParty 임시 기획 메모

이 파일은 구현 중인 기획, 여러 기능이 함께 참조하는 현재 계약, 실제 미정 TODO만
기록한다. 완료된 구현 내역과 테스트 결과는 코드·테스트·Git 이력을 원본으로 삼고
여기에 반복해서 보관하지 않는다.

- 사용자의 최신 명시적 지시가 이 문서보다 우선하며 충돌하면 이 문서를 바로잡는다.
- Notion은 사용자가 회의록 작성을 요청했을 때만 이 문서와 성공한 커밋을 근거로 갱신한다.

## 공통 현재 기준

- PC 온라인 고정 4인이며 실행 프로세스당 로컬 플레이어는 1명이다.
- 기본 게임은 15턴이며 전체 턴 수, 미니게임 큐 길이와 종료 턴은 같은 설정을 참조한다.
- 보드에서는 이동과 최종 칸 격투를 마친 뒤 칸 효과를 정산하고, 이어서 해당 턴의
  미니게임 공개 및 실행 단계로 진행한다.
- 서버가 체력, 열쇠, 골드, 미니게임 1위 횟수와 경기 진행 상태를 권한 있게 관리한다.
- 현재 종합 순위 비교 기준은 열쇠 수, 골드, 미니게임 1위 횟수 순이다.
- 인벤토리는 3슬롯이며 빈 상태로 시작한다.
- 플레이어 기본 이동 속도는 5m/s, `Left Ctrl` 저소음 걷기는 2.75m/s다.
- 플레이어 외형은 네트워크·충돌 루트와 분리된 `VisualRoot` 아래에 머리, 몸통,
  양손, 얼굴, 모자와 옷 앵커를 둔다. 게임 규칙 연출은 이 앵커를 재사용한다.
- 비호스트 연결이 끊기면 서버가 최대 60초 동안 전체 시뮬레이션과 타이머를 정지하고,
  복귀하면 직전 상태에서 재개한다. 호스트 종료는 현재 세션 종료로 처리한다.
- 보드 공용 UI 원본은 `Assets/MazeParty/Prefabs/Board/UI/BoardCanvas.prefab`이다.
  런타임은 전용 직렬화 바인딩을 사용하며, 필수 참조와 UI 컴포넌트를 보호 계약으로
  유지한다. 바인딩 후에는 계층 이름을 바꿔도 런타임 동작에 영향을 주지 않는다.
- 모든 플레이 화면의 Canvas UI 원본은 `Assets/MazeParty/Prefabs` 아래의 프리팹으로
  관리한다. 런타임 코드는 프리팹 인스턴스를 참조해 값과 표시 상태만 갱신하며
  `new GameObject`, `OnGUI` 등으로 화면 UI를 조립하거나 누락 시 대체 UI를 만들지 않는다.
- UI 생성·재빌드 도구는 프리팹이 없을 때만 기본 디자인을 만들고, 이미 있는 프리팹의
  배치·색상·폰트·문구 등 디자이너 변경은 덮어쓰지 않는다. 새 UI도 전용 직렬화
  바인딩과 프리팹 계약 검사를 함께 추가한다.
- 런타임 프리미티브와 새 경기장 재질은 Resources에 포함된 URP Lit 재질을 사용해
  Standalone 셰이더 스트리핑으로 마젠타가 되지 않게 한다.
- Standalone은 VSync를 끄고 목표 프레임을 60으로 설정한다.

## 현재 확정 기준: 보드 턴과 미니게임 직전

- 턴 탑뷰 5초, 카메라 하강 1초, 동시 행동 최대 3분, 상승·판정 유예 기본 5초,
  최종 칸·연쇄 격투, 칸 효과 4초, 미니게임 공개·READY·로딩 순으로 진행한다.
  2턴의 열쇠 상점 최초 등장 연출은 해당 턴의 5초 탑뷰에 포함한다.
- 주사위는 D12(1~12)이며 결과 확정 뒤 월드 주사위 확인 시간은 1초다.
  최종 칸 격투 제한 시간은 1분이고, 보드 일반 행동 중 맨손 펀치를 허용한다.
- 3분 종료 시 주사위를 굴리지 않은 플레이어는 제자리에 남고 최대 5골드를 잃는다.
  주사위를 굴렸지만 이동이 남은 플레이어는 기본 5초 상승·판정 유예 뒤 남은
  이동 수 전체를 계산해 최종 칸 중앙으로 한 번에 이동한다. 중간 칸의 효과·상점
  구매·전투는 처리하지 않는다. 활성 열쇠 상점까지의 일방향 최단 경로를
  우선하고 동률이면 진행 방향 기준 가장 오른쪽 길을 택한다.
  시간 종료 당시 현재 칸이 상점이면 움직이지 않는다.
  상점이 없거나 도달 경로가 없으면 직전 진입 방향으로 직진한다. 직진 출구가
  없으면 역행을 제외한 첫 출구를 선택하고, 그런 출구도 없으면 제자리에 남는다.
- 열쇠 상점 동시 구매는 서버가 먼저 처리한 요청 한 건만 성공한다. 구매 즉시
  상점이 이동하며, 같은 플레이어도 새 상점을 다시 찾아가면 같은 행동 단계에서
  추가 구매할 수 있다. 열쇠 구매는 전원 카메라의 구매 연출과 별도 안내로
  알린다. 아이템 상점 구매는 별도 성공·실패 팝업 없이 재화와 재고 변화로 알린다.
- 여러 최종 칸 격투와 격투 순위의 완전 동률은 현재 서버의 결정적 순서
  (성립·도착 시각, 현재 종합 순위, 좌석 순서)를 사용한다. 여러 칸 후퇴는
  실제 이동 이력을 역순으로 따른다. 비참가자는 현재 격투 칸을 비추는
  자동 관전 카메라를 사용한다.
- 칸 효과는 모든 연쇄 격투가 끝난 뒤 각 플레이어의 최종 칸에서 턴당 한 번,
  서버 좌석 순서로 정산한다. 4초 정산 화면은 각 차례의 대상·효과·증감량을 표시한다.
- 미니게임 READY는 최대 60초 기다리고, 미응답자는 제한 시간이 끝나면
  서버가 자동 READY 처리한다. 네 명의 READY와 씬 동기화 뒤 플레이를 시작한다.
  로딩이 60초 안에 완료되지 않으면 네트워크 실패로 경기를 종료한다.
  재접속 전역 정지 중에는 이 제한 시간도 멈춘다. READY 화면에는 총 인원과
  네 명 각각의 준비·대기 상태를 표시한다. 미니게임 15종의 규칙 이미지는
  공통 스타일의 임시 카드로 제작해 연결하고, 최종 아트로 교체할 수 있게 둔다.
- 보드 일반 체력과 보호 효과는 미니게임 전후에 보존한다. 미니게임 내부에서는
  별도 체력·효과를 사용한다.
- 턴 시작 탑뷰의 본인→열쇠 상점 최단 경로는 북쪽 고정이며 주사위 결과 전의
  가상 도달 범위는 표시하지 않는다. 2026-09-23 변경: 보드 HUD 우측 상단의
  미니맵은 로컬 플레이어의 1인칭 시선 방향을 위로 삼아 자동 회전한다.
  서쪽을 보면 서쪽이 위에 오며 본인 표시는 항상 위를 향한다. 수동 확대·회전
  조작은 없다. 미니맵은 본인의 실제 월드 위치 중심 원형이며 반지름 2칸(16m)의
  정보만 표시한다. 범위 내 플레이어도 실시간 표시한다. M키는 북쪽 고정 전체 지도를
  열고 닫으며 게임 시간은 계속 흐른다. 전체 지도는 본인 위치만 표시하고 상대 위치는 숨긴다. 벽·일방향 출구·현재 칸 정보를 표시한다.
  4인 상태 패널에는 강제 이동·격투·상점 연출·
  사망 상태의 보조 아이콘을 각각 표시한다.
- 일반 체력이 0이 되면 사망 위치에서 1초간 입력을 잠그고 캐릭터가 쓰러진다.
  사망 판정 시점에 10초 아이템 피해 보호를 시작하며, 1초 연출 중에도 행동
  3분과 보호 시간이 흐른다. 사망 직전 골드의 30%(버림)를 차감하고 1골드
  이상이면 사망 위치에 개별 묘비를 남긴다. 1초 뒤 사망 위치에서 월드 직선거리가
  가장 가까운 리스폰 칸(동률은 좌표 순)으로 이동해 체력을 100으로 회복한다.
  남은 이동 수와 선택한 아이템은 유지한다. 같은 위치의 묘비는 서로 약간 떨어져
  각각 보이고, 우클릭으로 조준한 하나를 회수한다. 동시 회수는 서버 처리순
  첫 요청만 성공한다. 묘비는 턴 사이에 남으며 원래 소유자도 회수할 수 있다.
  열쇠 구매·재접속 전역 정지 중에는 사망 연출과 보호 시간이 멈추고,
  부활 10초와 격투 후 30초가 겹치면 더 늦게 끝나는 시각까지 보호한다.
  탑뷰 5초 정산 중 사망 연출이 남으면 해당 연출 종료까지 탑뷰를 연장한다.
- Pulse Blaster, Push Mine, Med Kit의 임시 아이템 3종은 이번 보드 점검에서
  사용하는 상태다. 2026-09-23 새 아이템 5종 기획 요청으로 교체·다양화를 구체화 중이며, 아래 아이템 기획 섹션의 확정/미정 구분을 따른다.
- 현재 구현은 시간 초과의 전체 남은 이동 정산, READY·로딩 60초 제한,
  4명 준비 상태, 15종 임시 규칙 카드, 탑뷰 최단 유향 경로, 시선 방향 기준 미니맵,
  범위 내 상대 실시간 위치·M키 전체 지도, 4인 상태 배지 및 칸 효과 정산 안내까지 연결했다.
  일반 사망·묘비·리스폰은 서버 권한으로 연결했고, 월드 묘비 프리팹과
  보드 씬 바인딩을 제작했다.

## 공통 미니게임 통합 계약

### 2026-09-20 에셋·코드 폴더 규칙

- 게임 에셋의 대분류는 `Assets/MazeParty/Code`, `Prefabs`, `Scenes`, `Sounds`다.
  각 대분류 아래를 `Multiplayer`, `Board`, `Minigames`로 구분하고, 미니게임
  전용 항목은 게임 이름별 하위 폴더에 둔다. 공통 미니게임 항목은 `Common`에 둔다.
- UI 프리팹도 새 `Prefabs` 구조로 이동한다. Editor 도구·테스트 코드를 포함한
  모든 프로젝트 코드를 `Code`로 정리한다. 재질·모델·텍스처의 `Art`와 Unity
  `Resources.Load` 경로가 걸린 `Resources`는 별도 기능 폴더로 유지한다.
- 미니게임 핵심 월드 오브젝트는 씬 직배치·런타임 임시 조립 대신 수정 가능한
  프리팹 원본과 씬 인스턴스 또는 직렬화된 런타임 프리팹 참조를 사용한다.
  재빌드 셋업은 기존 프리팹 디자인을 덮어쓰지 않는다. 인씬 `NetworkObject`
  루트는 서버/NGO 직렬화 계약을 위해 씬에 유지한다.
- 15종 모두 게임별 핵심 오브젝트 프리팹을 씬에 연결했다. 특히 격투의 경기장
  구조물, 스노우 스핀의 얼음 경기장, 탄막슈팅의 절벽 경기장은 각각 독립된
  구조 프리팹으로 분리해 바닥·경계·표식을 원본에서 수정할 수 있다.

- 현재 15종 미니게임이 카탈로그에 등록되어 있다. 목록과 라운드 설정의 기준은
  `MinigameCatalog`이며 이 문서에는 중복 목록을 유지하지 않는다.
- 보드 칸 효과 정산이 끝나면 탑뷰에서 교체 가능한 룰 이미지와 READY UI를 표시한다.
  네 플레이어의 READY와 네트워크 씬 동기화가 모두 끝난 뒤 실제 플레이를 시작한다.
- 미니게임은 NGO additive scene으로 로드하고 세션 종료 시 Board와 함께 해제한다.
- 서버가 미니게임의 논리 위치, 입력 승인, 상태, 타이머와 순위를 권한 있게 계산한다.
- 전체화면, 1인칭, 전투와 미니게임 카메라는 이동 보간 없이 즉시 컷으로 전환한다.
- 실제 플레이 중에는 Board Canvas의 렌더링과 입력을 끄고 공통 미니게임 HUD 및
  게임별 필수 정보 HUD만 쓴다. 규칙 확인은 Board UI, 최종 결과는 별도 Canvas를
  사용한다.
- 별도 지시가 없는 미니게임은 라운드 시작 전 3초 카운트다운과 종료 후 4초 결과
  표시를 공통으로 사용한다.
- 내부 라운드를 사용하는 미니게임의 3/2/1/0 경기점수는 해당 미니게임의 최종 순위를
  계산하기 위한 값일 뿐 골드가 아니다. 미니게임이 끝나 최종 순위가 확정된 뒤 별도의
  순위별 골드 보상을 한 번만 지급한다.
- 최종 3/2/1/0골드를 사용하던 과거 구현과 문서는 라운드 점수를 임시로 재사용한
  잘못된 기준이다. 기존 및 신규 미니게임의 공통 최종 보상은 1위부터 10/6/3/0골드이며,
  최종 1위의 미니게임 승수를 1 올린다.
- 같은 판정값의 동률은 미니게임에서 정한 추가 기준 뒤 서버 처리순으로 확정해
  모든 클라이언트가 같은 고유 순위를 갖게 한다.
- 미니게임 중 비호스트 재접속 대기는 Board UI 대신 공통 HUD에서 같은 60초 서버
  타이머를 표시한다.
- 모든 미니게임의 시간 표시는 최종적으로 Board 씬의 단일
  `MinigameCommonHud.prefab`에서 제공한다. 기존 `MinigameTimerDial`은 그 안의
  시간 표시 구성요소다.
  가운데에는 남은 시간을 표시하고 바깥 원형 선은 경과 시간에 따라 12시 방향부터
  반시계 방향으로 사라진다. 같은 공통 HUD에서 시작 숫자 카운트다운과 해당하는
  게임의 라운드/문제 번호도 표시한다. 기존 별도 HUD가 없던 폭탄돌리기,
  스노우 스핀, 격투에도 공통 시간 UI를 적용한다.
- 2026-09-20 사용자가 승인한 UI 이관으로 기존 9개 게임별 HUD 프리팹에서는
  중복 타이머·라운드 및 불필요 정보를 제거하고 게임별 필수 정보만 남겼다.
  스테이블 풋팅·풍선 불기·선물 줍기의 라운드 결과 패널은 각 HUD와 다른 Canvas를
  사용한다. 술래잡기·레이스·탄막슈팅의 타이머 전용 씬 HUD 인스턴스는 제거했지만
  원본 프리팹 자산은 복구용으로 보존한다. 15개 게임의 플레이 시간과 해당 게임의
  라운드는 Board 씬의 단일 공통 HUD가 표시하며 최종 순위·골드 결과는 별도
  `MinigameResultCanvas.prefab`에서 표시한다.
- 게임 시작 시 전체 턴 수만큼 미니게임 큐를 한 번 만들고 구현된 서로 다른 게임을
  한 번씩 넣는다. 구현 게임이 부족한 슬롯은 `SKIP`으로 채운 뒤 전체를 섞는다.
- 큐는 호스트의 `Application.persistentDataPath` 아래 논리 매치 ID별 JSON으로
  저장한다. 활성 매치 ID는 인증 프로필별 PlayerPrefs에 보관한다.
- 미래 큐 전체는 호스트만 보유하고 클라이언트에는 현재 공개 항목과 남은 블록 수만
  복제한다. 준비 화면의 블록 탑은 미래 항목을 `???`로 숨기고 최상단만 공개한다.
- 같은 경기를 재시작하거나 호스트 프로세스를 다시 실행해도 저장된 큐를 재사용한다.
  호스트가 명시적으로 방을 나가거나 15턴을 마치면 활성 큐를 정리한다.
- `MazeParty > Developer > Minigame Solo Tester`는 카탈로그의 미니게임을 1인 로컬로
  실행한다. 새 게임은 제작 씬과 런타임 컴포넌트를 재사용하도록 등록한다.
- 솔로 테스터는 원래 열린 씬과 Play Mode 시작 씬을 종료 후 복구하며 제작 씬 에셋과
  Build Settings를 수정하지 않는다. RPC와 4인 동기화는 별도 멀티플레이 테스트로
  검증한다.

## 진행 중 리팩터 기록 (2026-09-13)

- 미니게임 공통 텍스트 포맷(남은 시간/순위)을 `MinigameDisplayFormatter`로 정리해
  보드 UI와 각 미니게임 뷰에서 중복 호출을 제거했다.
- `MinigameCatalog`를 추가해 등록된 미니게임 메타데이터(표시명, 라운드 수, 라운드
  시간, 등록 ID 목록)를 한 곳에 정의했으며, `MinigameScheduleRules`는 이 카탈로그를
  통해 조회하도록 리팩터링했다.
- 2차 리팩터 대상으로 `BoardFlow`/`MinigameScheduleTower`의 미니게임 표시명 조회를
  카탈로그로 이관했으며, 상태별 색상·규칙 클래스 바인딩도 같은 방향으로 확장했다.
- 3차 반영: `BoardFlow`/각 미니게임 HUD 텍스트에서 시간 포맷(`FormatClock`)과
  등수 표기(`ToOrdinal`)를 `MinigameDisplayFormatter` 공통 유틸로 이동했다.
- 3차 반영: `NetworkMatchState`의 씬 조회를 `MinigameCatalog` 기반으로 변경해 새 미니게임
  등록 시 이름/씬/라운드 수/컬러를 카탈로그 한 곳에서 갱신하면 흐름 분기를 덜 수정하도록
  정리했다.
- 3차 반영: `OnlineSessionController`에서 미니게임 씬 언로드도 등록된 카탈로그 목록을 순회하도록
  변경해 추가/삭제되는 미니게임에 대응력을 높였다.
- 4차 반영: `NetworkMatchState`의 미니게임 런타임 제어(일시정지/재개/복귀/종료/시작)를
  직접 분기 호출에서 `MinigameRuntimeController` 레지스트리 순회 헬퍼로 통일해, 추후
  미니게임 추가 시 `NetworkMatchState` 수정 없이 `MinigameCatalog` 등록·기능 래퍼 추가만으로
  동작하도록 정리했다.
- 5차 반영: `NetworkPlayerAvatar`의 미니게임 입력 RPC를 직접 미니게임 State 호출에서
  분리해 `NetworkMatchState` 라우팅 API로 통합했다. `MinigameRuntimeController`에
  입력/행동별 위임(이동, 푸시, 풍선불기 홀드, 메인 액션, 소나, WrongWay 방향)을
  묶어, 새 미니게임 추가 시 공통 입력 브릿지 확장만으로 Avatar 의존도를 줄였다.
- 6차 반영: `NetworkPlayerAvatar` 로컬 입력 수집부에서 `Network*State.Instance` 직접
  참조를 제거하고, `NetworkMatchState`에 미니게임 공통 질의 API를 추가했다.
  (`CanCurrentMinigameAcceptInputForSlot`, `TryGetCurrentMinigameRoundAndInputEpoch`)
  이제 새 미니게임 추가 시 공통 입력 허용 여부/라운드·에폭 조회도 런타임 레지스트리 경유로
  통일된다.
- 7차 반영: `MultiplayerBootstrapTests`에 미니게임 런타임 레지스트리 정합성 테스트를
  추가했다. `RegisteredMinigameRuntimes`와 `MinigameCatalog`의 ID 일치, 미니게임 상태
  쿼리의 기본 폴백 동작을 회귀 테스트로 고정했다.
- 8차 반영: `MINIGAME_EXTENSION_CHECKLIST.md`에 추가 미니게임 등록 체크리스트를 작성해
  운영 지침으로 반영했고, 수동 계약 검증 항목을 분리해 정리했다.
- 9차 반영: `MinigameCatalog`를 참조하는 뷰/컨트롤러들에서 네임스페이스 누락 가능성을 점검해
  필요한 `using MazeParty.Gameplay.Minigames`를 보강, 타입 해석 실패로 인한 컴파일/콘솔 에러
  위험을 선제 제거했다.
- 10차 반영: Unity 콘솔에서 확인된 카탈로그 접근 제한 오류 21건과 WrongWay의 잘못된
  이동 입력 위임 1건, 계약 테스트의 `System.Array` 타입 해석 오류 1건을 수정했다.
  WrongWay는 방향 전용 입력 위임만 유지하며, 네트워킹 어셈블리에서 사용하는 카탈로그
  조회 API(라운드 수/씬 이름/타워 색)는 공개 계약으로 정리했다.
- 최종 검증(11차 분리 후 재실행): Unity 6000.6.0f1에서 컴파일 오류·경고 0건, 전체 EditMode 테스트
  90/90 통과, `OnlineBootstrap` Play Mode 초기화 스모크 테스트 오류·경고 0건을 확인했다.
- 현재 리팩터링 단계는 완료 상태다. 새 미니게임 추가 시
  `MINIGAME_EXTENSION_CHECKLIST.md`의 등록 절차를 기준으로 확장한다.
- 11차 반영: `NetworkMatchState` 안에 있던 미니게임별 delegate 런타임 등록부와
  `MinigameRuntimeController`를 제거했다. 공통 계약은 `IMinigameRuntimeAdapter`와
  `MinigameRuntimeAdapter<TState>`로, 각 미니게임의 수명주기·입력 처리는 개별
  어댑터 클래스로, 전체 등록과 일괄 pause/resume/reconnect/end 처리는
  `MinigameRuntimeRegistry`로 분리했다.
- `NetworkMatchState`는 현재 미니게임을 레지스트리에서 조회해 보드 흐름과 입력을
  중계하는 역할만 유지한다. 새 미니게임의 런타임 연동은 전용 어댑터 추가와
  레지스트리 등록으로 확장하며, 등록 ID 정합성 테스트는 공개 `RegisteredIds`
  계약을 사용해 private 구현 반사 의존성을 제거했다.

## 현재 구현 기준: 무궁화꽃이 피었습니다

- Machine Party의 Table Manners처럼 신호 전환을 보고 멈추는 Red Light / Green Light
  형식의 4인 경쟁 미니게임이다.
- 출발선과 도착선이 있는 공용 경기장에서 진행하고 플레이어끼리 충돌하고 밀릴 수 있다.
- 각 클라이언트는 자신의 플레이어를 따라가는 직교 개인 추적 탑뷰를 사용하며 주변의
  다른 플레이어도 함께 볼 수 있다.
- 한 경기는 3라운드이며 매 라운드는 3초 카운트다운, 60초 플레이, 4초 결과 표시로
  진행한다.
- 감시자가 등을 돌린 Green Light는 매번 1.5~4초 사이에서 서버가 무작위로 정한다.
- Red Light 전환 전 0.35초의 감시자 회전 예고를 보여주고 회전이 끝난 순간부터
  이동 위반을 판정한다. Red Light는 1~2.5초 사이에서 무작위로 유지한 뒤 반복한다.
- 서버는 Red Light 중 플레이어의 자발적 이동 입력을 위반으로 판정한다.
  다른 플레이어에게 밀리거나 물리 흔들림과 네트워크 보정으로 위치가 바뀐 것은
  위반에서 제외한다.
- Red Light가 시작된 뒤 0.15초는 입력 해제를 위한 허용 시간으로 두며 그 뒤 계속된
  이동 입력부터 위반으로 처리한다.
- 같은 Red Light 구간에서는 한 플레이어에게 최대 한 번만 위반을 부여한다. 입력을
  계속 누르고 있으면 다음 Red Light에서 다시 적발될 때 두 번째 위반이 된다.
- 첫 위반은 Minefield의 1회 피격처럼 몸통과 옷을 숨기고 양손을 머리 옆에 두는
  경고 상태로 만든다. 이후 이동 속도는 걷기 속도인 2.75m/s로 낮아진다.
- 경고 상태에서 다시 위반하면 즉시 탈락한다.
- 한 명이 도착선에 처음 도달하면 그 라운드를 즉시 종료한다. 제한시간이 먼저 끝나도
  그 시점의 상태로 순위를 정한다.
- 순위는 생존 여부를 먼저 비교해 모든 생존자를 탈락자보다 앞에 두고, 각 집단에서는
  출발선부터의 전진 거리가 먼 순서로 정한다. 같은 거리면 서버 처리순으로 확정한다.
- 라운드별 3/2/1/0점, 3라운드 합산 최종 순위, 10/6/3/0골드와 최종 1위 승수 +1은
  공통 미니게임 보상 계약을 사용한다.
- 감시자, 신호등, 룰 이미지와 효과음은 실제 아트·오디오로 교체 가능한
  플레이스홀더와 직렬화 앵커로 구현한다.
- 신규 additive scene, Build Settings, 미니게임 카탈로그, 15턴 큐와 Solo Tester에
  등록하고 기존 네트워크 준비·재접속·결과 흐름을 재사용한다.

## 현재 구현 기준: Stable Footing

- Machine Party의 Stable Footing처럼 6×8, 총 48칸의 공용 발판 경기장에서 진행한다.
  발판 문양은 X, 원, 사각형 세 종류다.
- 한 경기는 3라운드이며 매 라운드는 공통 3초 카운트다운, 최대 60초 플레이와
  4초 결과 표시를 사용한다.
- 각 사이클은 문양 셔플·안전 문양 공개 1초, 이동 4초, 오답 발판 낙하 2초,
  복귀 1초 순서다. 이동 시간은 사이클마다 0.25초씩 줄어 최소 2초까지 빨라진다.
- 안전 문양이 아닌 활성 발판은 판정 중 일시 낙하했다가 복귀한다. 매 사이클 서버가
  시드 기반으로 고른 활성 발판 2칸은 복귀하지 않아 경기장이 점차 줄어든다.
- WASD는 고정 카메라 기준 월드 방향 이동이며 기본 속도 5m/s를 사용한다. 좌클릭은
  마지막 이동 방향의 짧은 전방 범위에서 가장 가까운 상대를 미는 서버 권한 행동이다.
  쿨다운은 0.65초이고 피해나 기절 없이 수평으로 약 1.5칸 밀어낸다.
- 추락하면 즉시 해당 라운드에서 탈락한다. 한 명만 남거나 전원 탈락하거나 60초가
  지나면 종료한다. 생존자를 먼저 배치하고 탈락자는 늦게 추락한 순서로 배치하며,
  같은 서버 틱은 서버 처리순으로 고유 순위를 정한다.
- 모든 클라이언트는 경기장 전체를 담는 동일한 고정 직교 카메라를 사용한다.
  각 클라이언트는 자기 캐릭터에만 로컬 탑뷰 하이라이트를 표시한다.
- 라운드별 3/2/1/0점과 3라운드 합산, 최종 10/6/3/0골드 및 1위 승수 +1은 공통
  미니게임 보상 계약을 사용한다.
- 전용 additive scene, 프리팹 기반 `StableFootingHud`, Build Settings, 15턴 큐,
  재접속 일시정지와 Solo Tester 통합을 기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: 풍선 불기

- Machine Party의 Smoke Break를 변형한 4인 경쟁 미니게임이다. 네 플레이어는 공용
  고정 카메라에 모두 보이는 각자의 고정 스테이션에서 풍선을 분다.
- 한 경기는 3라운드이며 매 라운드는 공통 3초 카운트다운, 30초 플레이와 4초 결과
  표시를 사용한다.
- 좌클릭을 누르고 있는 동안 풍선 진행도가 초당 10% 증가한다. 한 번에 연속으로 불 수
  있는 시간은 최대 2초다.
- 2초 전에 좌클릭을 놓으면 1초 쿨다운을 적용한다. 2초 한도까지 계속 누르면 강제로
  멈추고 1.5초 패널티 쿨다운을 적용하며, 버튼을 한 번 놓은 뒤 새로 눌러야 다시 불 수
  있다.
- 플레이 중 풍선을 불고 있지 않은 모든 시간에는 진행도가 초당 3% 감소하며 0% 아래로
  내려가지 않는다. 진행도가 100%에 도달하면 풍선이 자동으로 터지고 그 플레이어의
  해당 라운드 조작과 완주 순위를 확정한다.
- 풍선을 터트린 플레이어가 먼저 배치되고 터트린 서버 시간과 서버 처리순으로 순위를
  정한다. 30초 안에 터트리지 못한 플레이어는 종료 시 진행도가 높은 순서, 이후 서버
  플레이어 순서로 배치한다. 전원이 터트리면 라운드를 조기 종료한다.
- 라운드별 3/2/1/0점과 3라운드 합산, 최종 10/6/3/0골드 및 1위 승수 +1은 공통
  미니게임 보상 계약을 사용한다.
- 본인 캐릭터에는 로컬 전용 하이라이트를 표시하고 모든 캐릭터 머리 위에 플레이어
  이름을 표시한다. HUD와 스테이션 이름표·진행 표시는 각각 수정 가능한 프리팹 원본과
  직렬화 바인딩을 사용한다.
- 전용 additive scene, Build Settings, 15턴 큐, 재접속 일시정지와 Solo Tester 통합을
  기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: 선물 뺏기

- Pummel Party의 선물 뺏기를 변형한 4인 개인전이다. 네 플레이어는 대칭적인 개인
  영역이 있는 공용 경기장에서 같은 고정 직교 카메라를 사용한다.
- 한 경기는 2라운드이며 매 라운드는 공통 3초 카운트다운, 60초 플레이와 4초 결과
  표시로 진행한다.
- 라운드 시작 시 중앙의 중립 위치에 선물 10개를 배치하고, 플레이 시작 후 15초,
  30초와 45초에 각각 3개를 추가해 라운드마다 총 19개를 사용한다.
- WASD는 카메라 기준 이동이며 선물이 없을 때는 기본 속도 5m/s, 한 개를 들고 있을
  때는 걷기 속도 2.75m/s를 사용한다. 플레이어끼리 충돌하고 몸으로 길을 막을 수 있다.
- 한 번에 선물 한 개만 들 수 있다. 기절하지 않은 빈손 플레이어가 느슨한 선물에
  닿으면 자동으로 줍고, 자기 영역에 들고 들어가면 보관 위치에 자동으로 내려놓는다.
  보관한 소유자는 실수로 다시 줍지 않지만 상대는 접촉해 훔칠 수 있다.
- 선물을 들고 좌클릭하면 바라보는 방향으로 던진다. 날아가는 선물은 착지 전에는
  주울 수 없고 첫 플레이어에게 맞으면 그 자리에 떨어진다. 맞은 플레이어는 1초간
  기절하고 들고 있던 선물도 떨어뜨린다. 던진 플레이어에게는 0.25초 자기 충돌
  면역을 적용한다.
- 빈손으로 좌클릭하면 0.65초 쿨다운의 짧은 전방 밀기를 한다. 맞은 상대는 작은
  넉백과 함께 0.5초간 기절하고 들고 있던 선물을 떨어뜨린다. 기절 중에는 이동,
  줍기, 던지기와 밀기를 할 수 없다.
- 떨어뜨린 선물은 원래 운반자가 0.25초간 다시 주울 수 없다. 경기장 밖으로 나간
  선물은 중앙의 중립 위치로 되돌린다.
- 라운드 종료 시 자기 영역에 보관한 선물 수가 많은 순서로 순위를 정한다. 같은
  개수면 라운드 중 선물을 영역에 유지한 누적 시간, 이후 서버 플레이어 순서로
  고유 순위를 정한다. 들고 있거나 날아가는 선물은 점수에 포함하지 않는다.
- 라운드별 3/2/1/0점을 주고 두 라운드 점수 합계로 최종 순위를 정한다. 점수 합계가
  같으면 두 라운드에서 보관한 선물 수의 합계, 마지막 라운드 순위, 서버 플레이어
  순서로 판정한다. 최종 10/6/3/0골드와 1위 승수 +1은 공통 보상 계약을 사용한다.
- 본인 캐릭터에는 로컬 하이라이트를 표시하고 플레이어 이름과 각 영역의 소유자·선물
  수를 표시한다. 화면 HUD와 월드 표시는 수정 가능한 프리팹 원본과 직렬화 바인딩을
  사용한다.
- 전용 additive scene, Build Settings, 15턴 큐, 재접속 일시정지와 Solo Tester 통합을
  기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: 땅따먹기

- Pummel Party의 영역 색칠 형식을 참고한 4인 개인전이다. 네모난 공용 경기장의
  네 모서리에서 시작하며 모든 플레이어가 같은 고정 직교 카메라를 사용한다.
- 한 경기는 단판이며 공통 3초 카운트다운, 60초 플레이와 4초 결과 표시를 사용한다.
- WASD로 이동하는 동안 원형 붓으로 지나간 경로를 끊김 없이 색칠한다. 화면에는 칸을
  노출하지 않고 내부 고해상도 소유권 마스크로만 면적을 판정한다.
- 상대가 색칠한 영역 위를 지나가면 해당 영역의 소유권을 즉시 덮어쓴다. 점수는 현재
  보유 면적 비율을 전체 1000점으로 환산한 값이며, 누적 획득량이 아니다.
- 플레이어끼리 충돌하고 몸으로 길을 막을 수 있다. 논리 이동을 경기장 안으로 제한하고
  네 방향 경계벽을 표시해 경기장 밖으로 떨어지지 않는다.
- 제한시간 종료 시 현재 보유 면적이 큰 순서로 고유 순위를 정하고, 같은 면적은 서버
  플레이어 순서로 확정한다. 최종 10/6/3/0골드와 1위 승수 +1은 공통 보상 계약을 사용한다.
- 본인 캐릭터에는 로컬 하이라이트를 표시하고 모든 캐릭터 위에 플레이어 이름을 표시한다.
  플레이 중 HUD는 남은 시간과 네 플레이어의 이름·현재 점수만 표시하며 나머지 상태 정보는
  로그로 남긴다.
- 서버 권위 소유권은 칠해진 여부와 2비트 소유자 정보로 압축 복제해 재접속한 클라이언트도
  현재 전체 도색 상태를 복원한다.
- 전용 additive scene, 프리팹 기반 `TerritoryPaintHud`, Build Settings, 15턴 큐,
  재접속 일시정지와 Solo Tester 통합을 기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: 술래잡기

- 4인 비대칭 추격전이며 한 경기에서 네 플레이어가 시드 기반 무작위 순서로 한 번씩
  술래를 맡아 총 4라운드를 진행한다.
- 각 라운드는 공통 3초 카운트다운, 최대 60초 플레이와 4초 결과 표시를 사용한다.
  술래가 도망자 세 명을 모두 잡으면 즉시 라운드를 종료한다.
- 도망자는 WASD로 걷기 속도 2.75m/s로 이동하고, 살아 있는 세 도망자를 한 화면에
  담는 같은 3인칭 그룹 카메라를 공유한다. 잡힌 플레이어는 해당 카메라로 관전한다.
- 술래는 5m/s로 이동하며 본인 시점의 1인칭 카메라와 마우스 시점을 사용한다.
  좌클릭 공격이 짧은 전방 범위의 도망자에게 적중하면 한 번에 잡는다.
- 플레이어끼리 충돌하며 경기장 경계와 네 개의 대칭 엄폐물에 막혀 밖으로 떨어지지 않는다.
- 술래가 전원을 잡으면 라운드 점수는 술래 3점, 도망자 0점이다. 제한시간까지 한 명이라도
  살아남으면 술래 0점, 잡힌 도망자 1점, 생존 도망자 2점이다.
- 4라운드 합산점수로 최종 순위를 정하고 동률은 서버 플레이어 순서로 확정한다.
  최종 10/6/3/0골드와 1위 승수 +1은 공통 보상 계약을 사용한다.
- 모든 캐릭터 위에 이름을 표시하고 살아 있는 로컬 도망자만 하이라이트한다. 플레이 중
  화면 HUD는 공통 원형 타이머만 표시하며 역할, 잡기, 점수와 결과는 로그로 남긴다.
- 전용 additive scene, 프리팹 기반 `TagChaseHud`, Build Settings, 15턴 큐,
  재접속 일시정지와 Solo Tester 통합을 기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: 레이스

- 네 플레이어가 세로 방향의 독립된 네 레인 출발선에 한 명씩 배치되는 4인 경쟁
  미니게임이다. 모든 플레이어는 경기장 전체를 담는 같은 고정 직교 카메라를 사용한다.
- 한 경기는 총 3라운드이며 각 라운드는 3초 카운트다운, 최대 60초 플레이와 4초 결과
  표시로 진행한다.
- A와 D의 새 키 입력을 번갈아 누를 때마다 한 칸에 해당하는 전진 횟수 1을 얻는다.
  같은 키를 연속으로 누르거나 키를 계속 누르고 있는 입력은 전진하지 않는다.
  따라서 `aad`는 2회, `ada`는 3회 전진으로 판정한다.
- 라운드마다 유효 전진 500회에 먼저 도달한 플레이어가 나오면 즉시 종료한다. 제한시간이
  먼저 끝나면 유효 전진 횟수가 많은 순서로 순위를 정하고, 같은 횟수는 서버 처리순으로
  고유 순위를 확정한다.
- 라운드별 순위에 3/2/1/0점을 주고 세 라운드 합산점수로 최종 순위를 정한다. 최종
  동률은 서버 플레이어 순서로 확정하며 최종 10/6/3/0골드와 1위 승수 +1은 공통 보상
  계약을 사용한다.
- 각 캐릭터 위에 이름을 표시하고 본인 캐릭터에만 로컬 하이라이트를 표시한다. 플레이 중
  화면 HUD는 공통 원형 타이머만 표시하며 진행 횟수, 라운드 순위와 합산점수는 로그로
  남긴다.
- 전용 additive scene, 프리팹 기반 `RaceHud`, Build Settings, 15턴 큐, 재접속 일시정지와
  Solo Tester 통합을 기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: ASD 암기 미니게임

- PC 온라인 4인이 같은 카메라 구도를 보는 단판 미니게임이다.
- 한 판 안에 10번의 문제 제시·입력 차례가 있으며 문제 길이는 차례별
  `5/5/5/6/6/6/7/7/7/8`이다. 이 10번은 별도 골드를 주는 라운드가 아니다.
  서버가 A/S/D로 문제를 만들고 `AADSS`처럼 같은 문자가 반복될 수 있다.
- 같은 키의 3연속 `AAA`/`SSS`/`DDD`는 생성하지 않는다. 길이 7·8에서는 각 문자의
  총출현을 2회 이하로 제한할 수 없으므로 비연속 3회 이상은 허용하며,
  한 판 안에서 완전히 동일한 전체 문제도 다시 출제하지 않는다.
- NPC가 문제를 한 번 먼저 보여주고 A=고음, S=중음, D=저음으로 들려준 뒤 문제를
  숨긴다. 실제 AudioClip은 추후 교체할 수 있는 직렬화 참조로 준비한다.
- 입력 단계는 최대 10초다. 플레이어가 기대 문자와 다른 키를 누르면 즉시 해당
  라운드 입력을 막고, 정답을 완성한 플레이어도 입력을 막은 뒤 다른 플레이어를 기다린다.
  플레이어 입력음은 다른 플레이어에게도 들린다.
- 입력 단계 종료 시 전원 입력을 막고 정답을 다시 표시한 뒤 오답자를 판정한다.
- 첫 오답에는 몸통을 잃고 이후 정답을 맞혀도 복구하지 않는다. 이후 어느 차례든
  두 번째 누적 오답이 발생하면 탈락한다. 시간초과의 미입력·부분 입력도 오답이다.
- 입력은 새 KeyDown만 인정한다. 기대 문자와 다른 첫 입력에 즉시 실패·잠금하고 정답을
  완성하면 즉시 성공·잠금한다. 모든 입력 가능 플레이어의 판정이 끝나면 10초를 기다리지
  않고 조기에 정답 공개 단계로 진행한다.
- 종료 조건은 열 번째 입력 차례 완료, 전원 탈락 또는 생존자 1명만 남은 시점이다.
- 열 번째 문제 정답자는 마지막 문제 정답 완료 서버 시각이 빠른 순서로 배치한다.
  열 번째 문제에서 첫 오답으로 몸통만 잃은 미탈락자는 열 번째 문제 정답자 아래,
  기존 탈락자 위에 둔다. 탈락자는 탈락 차례가 늦을수록 상위이며 같은 차례에서는
  실패 전 정답 접두 길이, 이전 문제들의 누적 입력 속도, 서버 처리순으로 확정한다.
- HUD에는 남은 시간, 현재 문제 번호, NPC 문제 표시와 플레이어별 A/S/D 입력을 둔다.
  각 플레이어의 실제 입력 문자열은 전원에게 실시간으로 공개한다.
- NPC는 문자를 한 글자씩 음과 함께 0.7초 간격으로 제시하고 마지막 문자 뒤 0.5초
  유지한 다음 숨긴다. 고정 스테이션의 캐릭터는 이동하지 않고 입력 반응만 보여준다.
  몸통 상실은 충돌·판정에 영향을 주지 않는 외형 변화이며 탈락자는 같은 화면에서 관전한다.
- 미니게임 시작 시 3초 카운트다운을 한 번만 사용한다. 각 문제는 NPC 시연, 최대 10초
  입력, 정답 2초 공개 순으로 진행하며 열 번의 문제 사이에는 별도 카운트다운을 두지 않는다.
  전체 순위 확정 뒤 최종 결과를 4초 표시한다.
- 열 번의 문제에는 개별 점수나 골드를 주지 않는다. 단판 최종 순위가 확정된 뒤 공통
  보상인 1~4위 10/6/3/0골드를 한 번만 지급하고 최종 1위의 미니게임 승수를 1 올린다.
- 서버 권위 순수 상태 모델과 NGO 런타임 어댑터, 전용 additive scene, 공용 카메라,
  네 고정 스테이션, 프리팹 기반 HUD, Build Settings, 15턴 큐, 재접속 일시정지와
  Solo Tester 통합을 기존 미니게임과 같은 방식으로 구현했다.
- A/S/D 고음·중음·저음 AudioClip 직렬화 슬롯과 NPC/플레이어별 AudioSource를 준비했으며,
  실제 음원 클립은 사용권이 확보된 에셋을 추후 할당한다.

## 현재 구현 기준: 바운싱 볼

- Pummel Party의 바운싱 볼 형식을 참고한 4인 개인전이다. 네 플레이어는 하나의
  고정 공용 카메라로 십자형 경기장을 본다. 각 플레이어는 자신의 골 앞 실드를
  A/D를 누르고 있는 동안 좌우로 이동한다.
- 한 판에 2라운드가 있으며 각 라운드는 3초 카운트다운, 60초 플레이와 결과 표시로
  진행한다. 라운드별 골 점수를 합산해 최종 순위를 정한다.
- 라운드 시작 시 공 3개가 경기장 중앙에서 각각 시드 기반 무작위 방향으로 발사된다.
  이때 공은 무색이며 어느 플레이어도 소유하지 않는다.
- 실드가 공을 치면 그 공은 실드 주인의 색으로 바뀐다. 공이 어느 플레이어의 실드를
  통과해 골인하면 그 공의 현재 색 주인이 1점을 얻는다. 무색공의 골인은 누구도
  득점하지 않는다.
- 골인한 공만 즉시 재생성한다. 재생성 위치는 골을 먹힌 플레이어의 현재 실드
  중앙이고, 처음부터 그 플레이어의 색을 띤 상태로 경기장 안쪽을 향해 발사된다.
  그 외의 공은 현재 운동 상태를 유지하므로 동시에 공 3개가 계속 진행된다.
- 최종 합계 골이 같으면 덜 실점한 플레이어가 앞서며, 그래도 같으면 서버 플레이어
  순서로 고유 순위를 정한다. 최종 순위에 따라 공통 10/6/3/0골드와 1위 승수 +1을
  한 번만 적용한다. 라운드별 골 점수를 골드로 바꾸지 않는다.
- 서버 권위로 이동·충돌·득점·순위를 판정하고 전용 additive scene, 공용 카메라,
  프리팹 기반 HUD, Build Settings, 15턴 큐, 재접속 일시정지와 Solo Tester 통합을
  기존 미니게임과 같은 방식으로 제공한다.

## 현재 구현 기준: 폭탄돌리기

- 4인 단판 생존전이다. 모든 플레이어는 같은 고정 카메라를 사용하고 WASD로 이동한다.
  네 플레이어의 위치는 폭발 후에도 유지하며, 탈락자는 관전한다.
- 매 폭탄은 동일한 맵 중앙에서 생성되며 생성 순간부터 서버 시드로 정한 20~25초
  퓨즈가 흐른다. 생존자가 폭탄에 닿으면 자동으로 줍는다.
- 폭탄 소유자는 기본 이동속도로, 빈손 플레이어는 더 느린 걷기속도로 이동한다.
  좌클릭은 바라보는 방향의 2D 히트박스에 들어온 본인 외의 가장 가까운 적 한 명을
  대상으로 한다. 폭탄 소유자의 클릭은 폭탄을 전달하고 받는 사람만 0.5초 기절한다.
  빈손 좌클릭은 대상 플레이어를 0.5초 기절시킨다. 현재 바라보는 방향은 마지막
  WASD 이동 방향이며 히트박스는 길이 2.2m, 반너비 0.9m, 클릭 간격은 0.65초다.
- 폭탄이 주인 없이 남아 있고 퓨즈의 절반이 지나면 가장 가까운 생존자를 추적한다.
  퓨즈 종료 시 소유자만 탈락하고 다음 폭탄을 중앙에서 생성한다. 생존자 한 명이
  남을 때까지 반복한다. 폭발 순간까지 소유자가 없으면 아무도 임의로 탈락시키지 않고
  중앙에 새 폭탄을 생성한다.
- 최종 순위는 마지막 생존자를 1위로, 탈락한 사람은 늦게 탈락할수록 상위로 정한다.
  라운드 점수는 없으며 최종 10/6/3/0골드와 1위 승수 +1을 한 번만 적용한다.
- 플레이 중 게임 전용 정보 HUD는 없지만 공통 HUD에 현재 폭탄의 남은 시간을
  표시한다. 폭탄의 빛도 남은
  퓨즈 50%/20%/10% 구간에서 점점 빠르게 깜빡여 위험을 알린다.
- 서버 권위 판정, 전용 additive scene, 공용 카메라, Build Settings, 15턴 큐,
  재접속 일시정지, Solo Tester를 기존 미니게임과 같은 방식으로 통합한다.

## 현재 구현 기준: 스노우 스핀

- 품멜파티의 Snowy Spin을 참고하되, 네 플레이어는 각각 자기 색상의 공을
  WASD로 굴린다. 이동 방향을 계속 유지하면 가속하며, 별도의 공격키 없이
  공끼리 충돌하여 상대를 밀어낸다. 공의 크기는 성장하지 않는다.
- 모든 플레이어가 같은 고정 탑다운 카메라를 보고 고정된 원형 얼음 경기장에서
  경쟁한다. 가장자리로 밀려 떨어지면 그 라운드에서 즉시 탈락한다.
- 한 경기는 3라운드다. 각 라운드는 3초 카운트다운 후 최대 60초 진행하며
  한 명만 남으면 조기 종료한다. 제한시간에 여럿 생존했다면 경기장 중앙에
  더 가까운 사람부터 높은 순위를 준다. 탈락자는 늦게 떨어질수록 상위다.
- 라운드 순위별 3/2/1/0점을 합산해 최종 순위를 정하며, 합산 동률은 후반
  라운드 순위부터 비교하고 그래도 같으면 서버 슬롯 순서로 고유 순위를 낸다.
  라운드 점수를 골드로 치환하지 않고 최종 10/6/3/0골드와 1위 승수 +1을
  한 번만 지급한다.
- 플레이 중 게임 전용 정보 HUD는 없고 공통 HUD에 시간·라운드를 표시한다.
  공통 인트로·최종 결과는 유지한다.
- 반경 8m의 고정 얼음 경기장, 반경 0.65m의 공, 반경 4.5m의 네 시작점을 사용한다.
  입력 방향 가속도는 13m/s², 최고속도는 6m/s이며 놓으면 감속한다.
  충돌·낙하·순위는 서버의 60Hz 결정론적 모델에서 판정한다.
- 전용 additive 씬·공용 Cinemachine 카메라·서버 상태·Solo 연습 모드와
  15턴 미니게임 큐, 재접속 일시정지, 공통 최종 보상 흐름에 통합했다.

## 현재 구현 기준: 미니게임 공통 시작 연출

- 등록된 15개 미니게임은 게임 시작 시 기존 서버 권위 3초 대기 시간을 공유한다.
  별도의 추가 대기 시간을 붙이지 않으며, 처음 시작할 때만 공통 숫자 UI에
  3→2→1을 표시하고 3초가 지나면 각 미니게임의 플레이 단계로 넘어간다.
- 카운트다운 시작부터 남은 시간이 1초가 될 때까지 로컬 플레이어의
  해당 미니게임 표현에 굵은 흰색 월드 공간 외곽선을 표시한다. 다른 플레이어
  화면에는 이 강조를 표시하지 않으며, 씬 종료·대상 소멸 시 정리한다.
- 숫자 UI는 `Prefabs/Minigames/Common/UI/MinigameCommonHud.prefab`을 원본으로 하여
  Board 씬에 독립 Canvas 인스턴스로 배치한다. 재접속 일시정지 시 같은
  남은 시간이 멈춰 UI와 강조의 시간 기준도 함께 멈춘다.

## 현재 구현 기준: 격투 미니게임

- 기존 격투 시스템을 활용하는 4인 개인전(1:1:1:1)이다. 모든 생존 플레이어는
  각자의 1인칭 시점으로 경기장 안에서 싸운다. 단판 60초이며 한 명만 남으면
  조기 종료한다. 탈락한 플레이어는 즉시 관전 카메라로 전환한다.
- 기존 서버 권위 주먹의 체력 100, 5 피해, 0.65초 간격, 사거리 2m, 넉백을
  재사용한다. 격투 경기장은 고정된 벽으로 둘러싸여 있으며 추락 판정은 없다.
  보드 격투의 타일 제한·패자 후퇴·다음 행동 보호와는 분리한다.
- 모든 플레이어의 체력 UI를 숨기며 본인 체력도 표시하지 않는다. 피격자 화면에만
  프리팹 기반 빨간 시각효과를 보여준다. 공통 시작 3초 카운트다운과 시작 2초 동안의
  로컬 플레이어 흰색 외곽선 강조를 적용한다.
- 시간 종료 시 생존자는 잔여 격투 체력이 높은 순, 탈락자는 늦게 탈락한 순으로
  순위를 매긴다. 같은 조건이면 서버 슬롯 순서로 고유 순위를 확정한다. 라운드 점수는
  없으며 최종 10/6/3/0골드와 1위 승수 +1을 한 번만 지급한다.
- 영속 네트워크 플레이어를 전용 additive 경기장으로 이동시켜 사용하고 종료 후
  보드 위치로 복귀시킨다. 재접속 일시정지와 15턴 미니게임 큐에 통합한다.
  개발용 Solo 연습 모드는 같은 경기장·격투 수치·순위 규칙을 사용하지만 실제
  네트워크 RPC/서버 권위 판정을 대신 검증하지는 않는다.

## 현재 구현 기준: 탄막슈팅 절벽에서 버티기

- 4인 공용 카메라 경기장에서 WASD로 이동하고, 마지막 이동 방향을 향해 좌클릭으로
  전방 2D 히트박스 안의 가장 가까운 상대 한 명을 민다. 클릭 간격은 0.65초다.
- 정해진 절벽 경기장 밖으로 떨어지면 목숨 수와 관계없이 즉시 탈락한다.
  바깥에서 시드 기반 무작위 방향으로 날아오는 포탄은 투사체 충돌로 판정한다.
  레이저는 직선 경로를 1초 예고한 뒤 0.5초 동안 발사한다.
- 포탄이나 레이저를 처음 맞으면 몸통을 잃고, 두 번째 맞으면 탈락한다.
  피격 뒤 1초 무적이며 이동속도는 변하지 않는다. 몸통과 목숨은 매 라운드 초기화한다.
- 동시에 활성인 포탄은 최대 5개, 레이저는 최대 2개다. 수량은 나중에 바꿀 수
  있도록 분리하고, 비활성 슬롯을 재활용하며 탄막 객체를 매번 파괴·재생성하지 않는다.
  현재 기본값은 포탄 1.5~2.5초 간격·초속 7.5m, 레이저 7~10초 간격이다.
- 3라운드 단판이며 각 라운드는 최대 60초, 한 명만 남으면 조기 종료한다.
  시간 종료 시 생존자는 남은 목숨이 많은 순, 그래도 같으면 중앙에 가까운 순으로
  높은 순위다. 탈락자는 늦게 탈락할수록 상위이며 최종 동률은 서버 슬롯 순서다.
  라운드 순위 3/2/1/0점을 합산하고 동률이면 후반 라운드 순위를 먼저 비교한다.
  골드는 라운드별로 지급하지 않고 최종 순위대로 10/6/3/0골드와 1위 승수 +1을
  한 번만 지급한다.
- 첫 라운드 시작에 공통 3초 카운트다운·2초 로컬 하이라이트를 적용하고,
  이후 라운드는 결과 표시가 끝나면 바로 시작한다. 프리팹 기반 공통 시간 UI에
  현재 라운드를 함께 표시하며 체력 UI는 표시하지 않는다.
- 15턴 미니게임 큐, 서버 권한 입력·판정, 재접속 일시정지, 최종 보상 지급에
  통합한다. 개발용 Solo 연습은 같은 경기장 자산·규칙 모델을 사용하지만 실제
  다중 클라이언트의 RPC·동기화를 대신 검증하지는 않는다.

## 실제 미정 TODO

- 15턴 종료 후 최종 결과 화면과 재화 이동
- 최종 장막 VFX, 엄폐물과 보드 레벨 디자인
- 고급 타격 VFX, 전투·발걸음 사운드와 실제 캐릭터 애니메이션
- 아이템 종류와 가격 다양화
- 사용권이 확보된 한글 폰트와 추가 눈·입·감정표현
- 호스트 마이그레이션
- 무궁화꽃이 피었습니다의 감시자, 신호등, 룰 이미지와 효과음 최종 에셋 교체
- Stable Footing의 발판·문양·안전 표식 아트와 효과음 최종 에셋 교체
- 풍선 불기의 풍선·스테이션·터짐 연출 아트와 효과음 최종 에셋 교체
- 선물 뺏기의 선물·개인 영역·투척 및 기절 연출 아트와 효과음 최종 에셋 교체
- 땅따먹기의 경기장·경계벽·도색 및 이동 연출 아트와 효과음 최종 에셋 교체
- 술래잡기의 경기장·엄폐물·잡기 및 피격 연출 아트와 효과음 최종 에셋 교체
- 레이스의 트랙·출발/도착선·주행 및 완주 연출 아트와 효과음 최종 에셋 교체
- ASD 암기 게임의 NPC·경기장 아트와 A/S/D 고음·중음·저음 클립 최종 에셋 교체
- 바운싱 볼의 실드·공·골대 아트, 충돌·득점 효과음과 연출 최종 에셋 교체
- 폭탄돌리기의 폭탄·폭발 VFX, 전달·기절 애니메이션과 효과음 최종 에셋 교체
- 스노우 스핀의 얼음 경기장·공·낙하 VFX 및 충돌·낙하 효과음 최종 에셋 교체
- 격투 경기장 환경 아트, 피격 화면 효과 디자인, 타격음과 관전 연출 최종 에셋 교체
- 탄막슈팅의 절벽 경기장·포탄·레이저 경고/발사·피격/낙하 효과음과 아트 최종 에셋 교체

## Notion 반영 체크포인트

- 2026-09-20 회의록과 커밋 `ccce77695a60f5d5f1c627049af7be7ab9305bb0`,
  `dc7714954b6f9ba96b479a9894ed626cfd61581a`, 기획서 v0.30까지 Notion
  반영을 완료했다. 신규 미니게임 5종의 규칙 페이지와 공통 HUD·결과창 규칙도 포함한다.
- 2026-09-20 사용자의 기획서 갱신 요청으로 커밋
  `f02360a86f8058e0b7732680f25833b6a2cc541c`의 프리팹·폴더 구조와
  남은 기능·테스트 플로우를 기획서 v0.31 및 미니게임 공통 페이지에 반영했다.
  이 요청에서는 프로젝트 회의록을 수정하지 않았다.
- 2026-09-23 사용자 요청으로 기획서 v0.33, 세부 룰, 구현 기록과 변경 이력을 최신화하고
  `2026-09-23 회의록`을 작성했다.
  회의록: https://app.notion.com/p/3e4c227fda0381dd9a1ac663e4ce072d
  반영 커밋: `a87fa8f6a1e62b568cee2d309370f89eb08ece8d`,
  `7968c9e14534f2fed2865972fd0a62a5fa07edec`,
  `a750c86bda39c2984319d071c28a71d528ea6afb`,
  `c2ec8aefe66f6c5613199828cba37ca32a76f44d`.
  지도 공개·회전, 개인 경로, 월드 프리팹 기준과 작업별 검증 범위를 기록하고 저장 후 재조회했다.
- 이후 내용은 자동 반영하지 않는다. 사용자가 회의록 작성을 요청하면 현재 확정안과
  성공한 Git 커밋의 실제 변경을 확인해 같은 날짜의 회의록에 누적한다.

## 2026-09-23 보드 미니맵 작업

- 제작 계획: UnitySimpleMiniMap 좌표 변환을 활용하고 기존 BoardCanvas 프리팹의
  행동 미니맵을 갱신한다. 플레이어 4명 마커·벽·출구·칸 정보는 미리 직렬화한다.
- 1인칭 시선에 맞춰 회전하며 턴 시작 탑뷰는 기존 북쪽 고정을 유지한다.
- 구현 완료: 중심 오프셋 보정, 4명 실시간 마커, 시선 회전, 현재 칸 정보, 벽/일방향 출구.
- 검증: 미니맵 및 공통 UI 계약 EditMode 8/8 통과. 북쪽/서쪽 프리팹 화면 확인.
  setup 재실행 전후 프리팹 해시 동일. 외부 4인 접속 검증은 수행하지 않았다.

- 추가 요청 구현 완료: 실제 플레이어 위치 중심의 반지름 2칸(16m) 원형 마스크,
  M키 전체 지도 토글(북쪽 고정·본인 위치만 표시), 보드 이탈 시 자동 닫힘. 지도 열람 중 게임은 계속 진행.
  반경 경계/중심 이동/지도 전환 및 프리팹 계약 7/7 통과. 원형/전체 지도 화면 확인.

- 미니맵 추가 확정: 칸 종류/효과 아이콘, 현재 칸에서 바로 이동 가능한 다음 칸의
  화살표만 표시(이동 수 0이면 숨김), 열쇠 아이콘과 최소 이동 칸수 표시.
  최소 거리는 일방향 통로 기준이며 상점 미등장/경로 없음/현재 칸 0을 구분한다. 구현 완료.

- 칸 정보 검증: 일방향 최단 거리/현재 칸 0/상점 이동 갱신/즉시 이동 화살표의
  이동 수 경계 및 기존 미니맵·전체 지도·프리팹 계약 총 8/8 통과. 아이콘은 코드 기반
  벡터 Graphic으로 프리팹에 배치하며 지도 회전 중에도 칸 아이콘은 정방향을 유지한다.

## 2026-09-23 열쇠 상점 바닥 경로 가이드

- 각 클라이언트에서 로컬 플레이어의 현재 칸부터 열쇠 상점까지 일방향 최단 경로를
  계산하고 칸 중앙을 잇는 바닥 점선에 노란 반구를 배치한다. 다른 사람 경로는 표시하지 않는다.
- 반구는 충돌·네트워크 동기화 없이 로컬 표시만 하며 간격 기본 0.8m, 지름 0.32m이다.
  턴 탑뷰·하강·행동 중 표시하고 이후 단계는 숨긴다. 칸·상점 변경 시 갱신하며 미등장,
  경로 없음, 이미 상점 칸인 경우 숨긴다. 사용한 프리팹 인스턴스는 풀로 재사용한다.
- 구현 완료: Board 씬에 로컬 경로 뷰와 노란 반구 프리팹을 연결했다.
- 검증: 경로 뷰/실제 보드 씬 계약/기존 미니맵 회귀 테스트 5/5 통과, 바닥 경로 화면 확인.
  Unity 콘솔 오류·경고 없음. 실제 4인 동시 접속에서 개별 화면 비교는 수행하지 않았다.


## 2026-09-23 보드 월드 오브젝트 프리팹화

- 사용자 확정 범위: 보드 환경·상호작용 오브젝트. 공용 플레이어 외형·1인칭 손·장착 아이템 모델은 제외한다.
- `Prefabs/Board/World`에 일반 칸 A/B, 시작 칸, 리스폰 칸, 열쇠 상점, 아이템 상점 1/2,
  플레이어별 이동 제한 벽, 배경 바닥 프리팹 9개를 추가했다. 기존 묘비·경로 반구·주사위·UI 프리팹은 유지한다.
- Board 및 BoardFlowTestbed의 기존 칸/배경을 프리팹 인스턴스로 변환했다. 좌표와 BoardTopology/Gate 참조를 보존했다.
- 상점/벽 런타임은 `Resources/MazeParty/Board/BoardWorldPrefabs` 카탈로그의 프리팹을 생성한다.
  씬 표시기와 NetworkPlayer에 카탈로그를 직렬화하고, 동적 테스트 플레이어도 같은 카탈로그를 사용한다.
- 상점 Visuals와 구매 판정 Collider를 분리했다. 월드 문구·강조 외형·벽 상태 색상은 프리팹에서 편집한다.
  아이템 상점 번호/재고 문구, 상점 위치/활성 상태, 벽 통과 상태와 개인 충돌 정책은 게임 코드가 갱신한다.
- 두 상점 구매 판정 MonoBehaviour를 개별 파일로 분리하여 아이템 상점 프리팹의 누락 스크립트 문제를 수정했다.
- 설치 메뉴 `MazeParty/Board/Install World Prefabs`와 보드 setup은 기존 프리팹·재질을 재사용한다.
  기존 D12 프리팹/재질을 setup에서 덮어쓰던 동작도 수정했다. 편집 안내는 World/README.md에 기록했다.
- 검증: 전체 EditMode 194/194 통과. 설치 재실행 전후 프리팹·재질·카탈로그 31개 해시 동일.
  실제 프리팹의 칸·상점·벽 렌더 확인, 콘솔 오류·경고 없음. 실제 4인 접속 비교는 수행하지 않았다.


## 2026-09-23 열쇠 상점 경로의 지도·탑뷰 표시 확인

- 확인 결과 기존 월드 반구 경로는 TurnOverview(주사위 전)·Descending·Action에서 표시되지만,
  미니맵/M키 전체 지도는 별도 UI 투영이어서 반구 경로 표시가 없었다.
- BoardCanvas 프리팹의 두 지도에 `BoardMapRouteGraphic`을 추가하고 기존 최단 거리 계산의
  방향성 경로 및 월드 반구의 칸 중앙 점 샘플링을 공유하도록 연결했다. 지도에서는 노란 원형 점선으로 표시한다.
- 미니맵은 실제 로컬 플레이어 중심·시선 회전·반지름 2칸 원형 마스크를 적용하고,
  M키 전체 지도는 북쪽 고정으로 전체 경로를 보여준다. 주사위 결과/남은 이동 수와 무관하게 경로를 표시한다.
- 미등장/도달 불가/이미 상점 칸인 경우 이전 점선을 지운다. UI는 프리팹에서 작성하며
  런타임은 경로 메시 데이터만 갱신한다. 색상·점 간격·최소 표시 크기는 프리팹 컴포넌트에서 편집한다.
- 탑뷰 표시 단계를 실제 경로 렌더 상태 테스트에 포함했다. 실제 BoardFramingAnchor의
  위치·시야각 및 출력 카메라 컬링 마스크로 렌더한 탑뷰에서 반구 경로가 보이는 것을 확인했다.
- 검증: 경로/미니맵 투영/실제 UI 프리팹·씬 계약 6/6 통과. 서쪽 시선 원형 미니맵,
  M키 전체 지도, 탑뷰 프리뷰 화면 확인. 콘솔 오류·경고 없음. 실제 4인 온라인 세션 검증은 수행하지 않았다.

## 2026-09-23 전체 지도 플레이어 공개 범위 변경

- HUD 원형 미니맵은 반지름 2칸 내 상대의 실시간 위치 표시를 유지한다.
- M키 전체 지도는 로컬 플레이어 본인 위치만 표시한다. 상대 마커와 강조 표시는 숨긴다.
- BoardCanvas 전체 지도 프리팹과 신규 생성 설정에 본인 전용 표시 정책을 적용했다.
- 검증: 지도·프리팹 관련 EditMode 5/5 통과. 좌석 0~3 각각의 본인 전환과
  플레이어 제거 후 마커 정리, HUD 상대 표시 유지 확인. 전체 지도 프리뷰에서
  본인 마커·칸 정보·열쇠 상점 경로 확인. 실제 4인 온라인 세션은 검증하지 않았다.
- 브랜치는 dev/Player를 유지했다. 이번 변경은 아직 커밋 및 Notion에 반영하지 않았다.

## 2026-09-23 실제 Relay 4인 온라인 검증

- dev/Player에서 개발 빌드 4개(호스트 1 + 클라이언트 3, 독립 인증 프로필)로 실제 Relay 방 생성,
  참가, READY 4/4, 보드 로딩, 각자 주사위/키보드 이동, 격투 미니게임과 골드 보상을 검증했다.
- 1차: 좌석 0~3 주사위 9/2/1/3, 시작 골드 10/10/10/10 → 칸 정산 10/10/13/13
  → 미니게임 보상 후 20/16/16/13. 4개 클라이언트의 골드 일치.
- 2차: 실행 파일을 완전히 재시작. 시드 1707520914와 미니게임 순서
  [14,6,11,15,1,4,8,10,9,2,3,7,13,12,5]가 저장 파일 복원 및 새 보드 진입 후에도 동일했다.
  등록된 15종이 각 1회 배치됐다. 다른 시드 1707520915의 순서는 달랐다.
- 두 실행 모두 2턴 주사위를 굴리기 전에 종료했다. 2차 상점 (1,1) 동기화,
  개인별 최소 거리 8/9/10/3칸과 월드·HUD·M 지도 점 개수 81/91/101/31 일치.
  유향 보드 간선을 별도 BFS로 확인했다. 탑뷰 반구 경로 및 지도 점선 렌더 확인.
- HUD는 반경 내 상대를 표시하고 M 지도는 각 클라이언트 본인 마커 1개만 표시했다.
- 미니게임 실플레이 범위는 ArenaCombat 1종의 READY·이동·60초 종료·순위·보상이며,
  나머지 14종 플레이와 격투 타격/탈락은 이번 범위에 포함하지 않았다.
- 발견 오류(미수정): 실행 파일 종료 때 ArenaCombatNetworkView.OnDisable:67의
  null 조건 연산자가 파괴된 Unity 객체를 걸러내지 못해 PlayerAvatarVisual.EnsureBuilt/
  DisableLegacyRenderers:612에서 NullReferenceException. 1차 host/c2, 2차 c1에서 확인.
- 관측 경고: 시작 시 Curl error 23 단발, 종료 시 JobTempAlloc 경고. 2차 오프스크린
  캡처에서는 URP 후처리 셰이더 누락 경고와 1인칭 오버레이 합성 차이가 있어 일반 창의
  최종 시각 품질을 통과로 간주하지 않는다. 연결·턴 진행·경로/마커 데이터 검증과 구분한다.
- 검증용 프로세스를 모두 종료하고 임시 probe 스크립트·meta를 게임 Assets에서 제거했다.
  브랜치 이동·커밋·Notion 수정은 하지 않았다. 상세 결과는 보조 작업 폴더의
  artifacts/online-validation-2026-09-23/REPORT.md 및 summary.json에 보관한다.

## 2026-09-23 플레이어 아이템 다양화 기획 및 구현

### 사용자 확정 요청

- 두배 주사위: 주사위 2개를 각각 굴리고 눈의 합만큼 이동한다. 기존 주사위는 D12다.
- 권총: 7발, 이동 중 좌클릭 발사, 피격 피해 20, 칸 경계 벽 통과, 직선 사거리 2칸(16m).
- 스나이퍼: 5발, 이동 중 좌클릭 발사, 우클릭 2배율 조준, 피격 피해 50,
  칸 경계 벽 통과, 직선 사거리 7칸(56m).
- 폭탄: 수류탄 포물선 투척, 직선 사거리 2칸(16m), 땅·일반 벽·플레이어에 닿으면 폭발.
  칸 경계 벽은 충돌에서 제외한다. 피해 80, 투척자도 가까우면 피해를 받는다.
- 지뢰: 조준한 지면에 좌클릭으로 설치. 설치자만 월드 시야·HUD 미니맵·M 지도에서
  위치를 볼 수 있다. 다른 플레이어가 근처를 지나면 폭발하여 피해 50.
- 해당 턴에 아이템을 사용하면 맨손 대신 해당 아이템을 들고, 다 쓰면 맨손으로 돌아간다.
- 기존 임시 아이템을 아래 5종으로 교체했다. 턴 시작 1개 선택 후 총기는 잔탄을 유지하고,
  선택 아이템의 미사용 수량·잔탄은 행동 종료 시 소멸한다.

### 추가 확정 — 사용자 답변 1-B / 2-A / 3-A

- 턴 시작에 아이템 1개를 선택하는 흐름을 유지한다. 선택한 아이템은 턴 종료 시
  잔여 탄약·미사용 수량까지 소멸한다. 선택하지 않은 인벤토리 아이템은 이 소멸 대상이 아니다.
- 폭탄: 아이템 하나당 1개, 폭발 반경 4m 안 모든 플레이어에게 거리 감쇠 없이 80 피해.
  투척자도 피해 대상이다.
- 지뢰: 아이템 하나당 1개, 조준한 지면 4m 이내 설치, 설치 1초 후 활성화.
  다른 플레이어가 반경 2m에 들어오면 발동하고, 반경 4m 안에 고정 피해 50을 준다.
  설치자 자신은 발동시키지 않지만 폭발 범위 안에서는 피해를 받는다.
- 이미 설치한 지뢰는 아래 4-A 확정에 따라 턴 종료 후에도 유지한다.

### 추가 확정 — 아이템 밸런스 데이터 SO 관리

- 사용자가 아이템 데이터를 ScriptableObject(SO)로 저장하고 한 폴더에 모으도록 요청했다.
- 아이템별 피해·탄약/수량·사거리·발사 간격·조준 배율·폭발/감지/설치 범위·활성화 지연,
  가격·등장 가중치 등 밸런스 수치를 아이템 정의 SO에서 편집하도록 구현한다.
- 추가 질문 1~9의 답변으로 수치를 확정하고 코드와 SO 에셋을 제작했다.
- 런타임은 SO의 정의 값을 읽고, 남은 탄약·소유자·사용/설치 상태는 별도 플레이어/매치
  상태로 관리하여 원본 SO를 수정하지 않는다. 판정은 서버 정의 값을 기준으로 한다.
- 기존 하드코딩 카탈로그와 상점·랜덤 보상·인벤토리 설명·실제 효과가 같은 SO를 참조하도록
  연결한다. 공통 아이템 데이터 폴더와 편집 안내를 제공한다.

### 추가 확정 — 사용자 답변 4-A / 5-A / 6-추천안

- 설치한 지뢰는 폭발하거나 게임이 끝날 때까지 유지한다. 미니게임 중에는 작동을
  멈추고 다음 보드 턴에 다시 활성화한다.
- 스나이퍼는 우클릭을 누르는 동안 2배율 조준한다. 주사위·상점·묘비 등 상호작용
  대상을 겨누었을 때는 기존 우클릭 상호작용을 우선한다.
- 기존 임시 아이템 3종을 새 5종으로 교체하고 상점 및 랜덤 아이템 보상에 함께 적용한다.
- 초기 가격: 두배 주사위 8G / 권총 10G / 스나이퍼 15G / 폭탄 8G / 지뢰 6G.
  등장 확률은 균등. 사용자 요청에 따라 SO에서 추후 가격·확률을 조정할 수 있게 한다.

### 추가 확정 — 7~9번 추천안 적용

- 총기는 클릭당 1발·즉시 명중 판정, 권총 발사 간격 0.25초 / 스나이퍼 1초.
  첫 플레이어만 피격, 부위별 배율·밀림 없이 지정 피해 20/50 적용.
  칸 경계 벽만 통과하고 일반 벽/지형은 차단한다.
- 두배 주사위는 D12 두 개가 모두 확정된 뒤 합계로 이동 시작.
  하나만 굴린 채 행동 시간이 끝나면 나머지를 서버가 굴려 합계로 기존 강제 이동 정산,
  둘 다 미사용이면 기존 미굴림 패널티 적용.
- 폭탄·지뢰 폭발 피해는 칸 경계 벽을 무시하되 일반 벽/지형에 가려진 플레이어는
  피해를 받지 않는다.

### 구현 결과

- dev/Player 유지. SO 원본은 `Assets/MazeParty/Resources/MazeParty/Items`의
  DoubleDice / Pistol / Sniper / Grenade / Mine 5개이며 README.md에 편집 방법을 기록했다.
- 가격·가중치·피해·탄약·사거리·간격·배율·폭발/감지 반경·활성화 지연·투척 수치 및
  프리팹 참조를 SO로 관리한다. 상점·랜덤 보상·인벤토리·사용 판정이 동일 카탈로그를 사용한다.
- 모델 원본은 `Assets/MazeParty/Prefabs/Board/Items`. 교체 가능한 임시 모델이며
  장착 중 손을 숨기고, 소모하거나 행동이 끝나면 손으로 복귀한다.
- 서버가 탄약·발사 간격·시점 위치/방향·피격/폭발·지뢰 감지를 판정한다.
  재접속 스냅샷에 잔탄·장착·발사 대기시간·각 주사위 진행을 보존한다.
- 스나이퍼는 실제 상호작용 대상만 우클릭 우선 처리하고, 일반 벽·플레이어를 겨눌 때는
  조준을 허용한다. 70도 기준 2배율 시 38.5907도로 변하며 해제 후 원래 시야각으로 복원한다.
- 두배 주사위는 좌석별 D12 두 개(씬 총 8개). 두 결과가 모인 뒤 2~24칸을 부여하며
  중복 결과를 거부하고, 시간 초과 시 하나만 확정된 경우 나머지를 서버가 보충한다.
- 지뢰 위치는 소유자 RPC로만 전달한다. 개인 월드 모델·HUD 미니맵·M 지도 그래픽에
  연결했고, HUD는 기존 반지름 2칸 제한, M 지도는 본인 지뢰 전체를 표시한다.
- 지뢰는 보드 행동 단계에서만 활성화 지연/감지가 진행되고 전역 정지 중 멈춘다.
  미니게임 동안 보존·비활성화하고 게임 종료 시 제거한다. 비행 중 수류탄은 행동 종료 시 정리한다.
- 런타임 Canvas 생성 없이 기존 BoardCanvas의 탄약/주사위 텍스트와 새 직렬화된 지도
  그래픽을 사용한다. 에디터 Upgrade 명령은 기존 SO 수치·프리팹 디자인을 덮어쓰지 않는다.

### 검증 결과 및 범위

- 최종 전체 EditMode 206/206 통과. 컴파일 오류 없음. SO/시드 결정론, 총알·폭발 차폐,
  두 주사위 중복·시간 초과, 스나이퍼 상호작용 우선순위, 실제 씬·프리팹 바인딩 포함.
- 실제 Relay 4인 개발 빌드에서 서버 fixture로 아이템/위치를 배치하고 검증:
  권총 20·스나이퍼 50 피해와 탄약/간격/위조 시점 거부, 폭탄 80 자가·대상 피해,
  지뢰 50 자가·대상 피해 및 소유자 비발동, 실제 D12 두 번 굴림 후 합계 이동 모두 통과.
- 지뢰 위치 데이터는 설치자 slot 1의 c3에서만 1개, host/c1/c2는 0개였다.
- 온라인 검증에서 발견한 플레이어 루트의 벽 관리 컴포넌트로 인한 피격 제외 오류를 수정하고
  회귀 테스트를 추가했다. 이전 ArenaCombat 종료 시 파괴된 객체 접근 오류도 수정했다.
- 테스트 fixture는 서버 API로 사격 요청을 넣었다. 실제 사용자 클릭 전체·온라인 재접속·
  지뢰의 미니게임 왕복 지속성은 이번 실행에서 재현하지 않았다. 스나이퍼 입력 우선순위
  마지막 수정은 온라인 실행 이후 적용했고 EditMode와 2배율 렌즈 계산으로 확인했다.
- 지뢰 지도는 프리팹/씬 바인딩과 소유자 데이터 전달까지 검증했으며 온라인 M 지도
  스크린샷은 별도 촬영하지 않았다. 상세 근거는 보조 작업 폴더의
  `artifacts/item-validation-2026-09-23/REPORT.md`, `summary.json`, 원본 로그에 보관했다.
- 테스트 프로세스와 임시 Assets 검증 코드를 모두 정리했다. 브랜치 이동·커밋·Notion 수정 없음.

## 2026-09-23 추가 아이템 4종 — 추천안 확정 및 구현 완료

### 사용자 요청
- 1~6만 나오는 주사위, 7~12만 나오는 주사위 추가.
- 위치변환기: 다른 플레이어를 선택해 서로 위치 교환. 시도 중 2초 행동 불가,
  시도 중 피격되면 아이템 소멸. 성공 시 대상에게 안내문구를 표시하고 즉시 이동.
- 은신망토: 해당 턴 동안 다른 플레이어 시야 및 미니맵에서 보이지 않음.
- 기존 SO 한 폴더 관리 및 턴 시작 아이템 1개 선택 정책을 기준으로 추가 설계한다.

### 확정 — 사용자 전체 추천안 승인, 은신은 격투에서 해제
1. 범위 주사위: 턴 시작 선택 시 기존 D12 한 개의 결과 범위를 변경하고 각 눈은 균등 확률.
2. 위치변환 대상: 목록에서 선택, 거리 제한 없음. 은신 중이어도 선택 가능하되 위치는 공개하지 않음.
   사망·부활 연출 중인 대상은 제외. 2초 동안 사용자만 행동 잠금, 대상은 자유롭게 행동.
3. 위치변환 결과: 완료 시점의 실제 위치와 현재 칸만 교환. 각자의 시선·남은 이동 수·
   주사위 결과는 유지하며 이동 횟수를 소모하지 않음.
4. 위치변환 취소: 보호에 막힌 명중은 취소하지 않고 실제 HP 감소 시 소멸.
   대상 사망/행동 단계 종료 시 취소·소모. 행동 타이머는 계속 흐르고 전역 정지 시 시전도 정지.
5. 은신망토: 좌클릭 사용 즉시 발동, 보드 행동 단계 종료 시 해제(최종 칸 격투 전).
6. 은신: 공격·피격에도 유지, 사망 시 해제. 캐릭터·장착물·이름표·그림자·미니맵 마커 숨김.
   발소리·충돌·피격·지뢰 감지는 유지하며 본인에게는 은신 상태 안내.
7. 초기 가격: 1~6 주사위 5G / 7~12 주사위 8G / 위치변환기 12G / 은신망토 10G.
   기존 아이템과 동일 등장 가중치로 상점·랜덤 보상에 추가하고 SO로 조정 가능하게 함.
- 은신은 행동 종료 전환에서 즉시 해제하여 최종 칸 격투에 남지 않는다.


### 추가 4종 구현 및 검증 결과
- dev/Player 유지. LowDice / HighDice / PositionSwapper / Cloak SO와 교체 가능한
  장착 프리팹을 추가하여 전체 9종을 동일 카탈로그·상점·랜덤 보상에 연결했다.
- SO에 DiceMinimum / DiceMaximum / CastDuration을 추가했다. 9종 SO는 기존
  `Assets/MazeParty/Resources/MazeParty/Items` 폴더에 모았으며 README를 최신화했다.
- 위치변환 목록·시전 상태·은신 상태·대상 안내는 BoardCanvas 원본 프리팹과
  BoardUtilityItemView 직렬화 바인딩을 사용한다. 목록에 위치를 공개하지 않는다.
- 서버가 시전·이동/주사위/상점 입력 잠금·피격 취소·교환을 판정한다.
  아직 굴리지 않은 주사위도 새 위치로 이동하며 시선과 남은 이동 수를 유지한다.
- 재접속 스냅샷에 은신, 범위 주사위, 시전 대상 좌석·남은 시간을 보존한다.
- 전체 EditMode 207/207 통과. 상태 경계/일시정지/취소 및 실제 UI 프리팹·씬 계약 포함.
  Windows 개발 빌드 성공, 컴파일 오류 없음.
- 실제 Relay 4인 개발 빌드에 서버 fixture로 아이템/위치를 배치해 검증했다.
  1~6/7~12 각각 1,000회 범위 샘플 및 실제 월드 주사위 굴림·소모 통과.
  소유자 RPC 시전, 2초 전 교환 방지, 양쪽 위치 교환·이동 수 보존,
  미사용 주사위 이동, 실제 피해 취소·소모, 은신 상태 복원 모두 통과.
- 은신한 slot 1은 host/c1/c2에서 world model 비활성·미니맵 마커 제외,
  소유자 c3는 자신의 미니맵 마커 유지. 모든 클라이언트에서 hitbox 활성 유지.
  행동→AscendingResolve 전환에서 은신과 진행 중 시전을 해제한 뒤 전원 동기화 확인.
  위치 교환 대상 안내는 해당 소유자 c3만 수신했다. 4개 플레이어 로그에 예외 없음.
- 검증 범위 제한: 아이템 장착과 배치는 서버 fixture이며 모든 실제 마우스 동작,
  온라인 연결 끊김→재접속 전체 흐름은 재현하지 않았다. 스냅샷 복원 경로는 검증했다.
  대상 사망/전역 정지 취소·시간 경계는 핵심 상태 테스트로 확인했다.
- 첫 검증의 동일 턴 재굴림 실패는 fixture의 match rolled mask 미초기화였다.
  fixture 수정 후 실제 게임 재굴림 방지 규칙을 변경하지 않고 재실행 통과했다.
- 보조 작업 폴더 `artifacts/utility-validation-2026-09-23/REPORT.md`, summary.json,
  원본 타임라인·스크린샷에 근거를 보관했다. 검증 프로세스와 임시 Assets 코드를 정리했다.
  브랜치 이동·커밋·Notion 수정 없음.


## 2026-09-23 표정 커스터마이징·손 감정표현 — 구현 완료

### 사용자 확정 요청
- 표정은 스프라이트를 활용한 고정 표정을 사용한다.
- 손 감정표현은 손을 교체하는 방식이며 교체된 손을 1초 유지한다.

### 현재 구조
- PlayerAppearanceState에 EyeId/MouthId가 있으나 현재 Sanitized에서 0으로 고정한다.
- PlayerAvatarVisual은 눈·입을 기본 입체 파츠로 표시한다.
- 로비 외형 저장/온라인 동기화와 1인칭·상대 시점의 손/아이템 표시 구조가 있다.

### 확정 — 원형 선택창 변경 외 전체 추천안 승인
- 표정은 얼굴 전체 스프라이트를 로비에서 선택·저장한다. 임시 표정 4종: 기본/웃음/화남/놀람.
- 손 3종(엄지/V/하트)은 교체 가능한 3D 프리팹으로 제작한다.
- T를 누른 채 원형 선택창에서 마우스를 끌어 선택하고 T를 놓으면 발동한다.
  중앙에서 놓거나 ESC/우클릭하면 취소. 선택 중 카메라 시점 전환·회전 없음.
- 맨손일 때 로비·보드 행동 중 사용. 본인 1인칭과 상대 시점에 모두 표시한다.
  격투·미니게임·사망·위치변환 시전·전역 정지 중은 사용 불가.
- 손 교체는 서버 기준 1초. 이동 허용, 공격/상호작용 시 즉시 취소,
  재입력으로 1초 연장 불가, 은신 중 상대에게 손이 노출되지 않는다.
- 표정/손은 SO 목록 및 스프라이트/프리팹 참조로 관리한다.
- 기존 색상/모자 저장을 유지하며 표정 필드를 추가하고 구버전 프로필을 읽는다.


### 표정·감정표현 구현 및 검증
- dev/Player 유지. PlayerExpressions.asset SO, 고정 얼굴 스프라이트 4종,
  두 손을 포함한 감정표현 프리팹 3종을 추가했다.
- 데이터/스프라이트: `Assets/MazeParty/Resources/MazeParty/Expressions`.
  손 모델: `Assets/MazeParty/Prefabs/Multiplayer/Expressions`.
- 로비 표정 화살표·미리보기와 T 원형 선택창은 LobbyCanvas/BoardCanvas 원본 프리팹에
  직렬화 바인딩으로 추가했다. 선택 중 배경 UI 클릭 차단, 중앙/ESC/우클릭 취소,
  T 해제 발동, 마우스 시점 고정 및 종료 프레임의 시점 튐 방지를 구현했다.
- 서버가 감정표현 ID·종료 시각을 동기화한다. 1초 종료, 재입력 연장 거부,
  아이템 장착/비허용 단계 거부, 공격·상호작용 취소, 은신 시 상대 손 숨김을 적용했다.
- 표정 ID를 외형 네트워크 상태와 프로필 v2에 추가하고 v1 프로필의 색상·모자를 보존한다.
- 전체 EditMode 209/209 통과. 저장 호환/손 지속시간 경계/원형 방향 선택,
  실제 UI 프리팹·씬 바인딩 및 에셋 참조를 검증했다.
- Windows 개발 빌드와 실제 Relay 4인 검증 통과. T/마우스 입력으로 선택창 열기,
  시점 유지, T 해제 엄지 발동, 중앙 취소 확인. V/하트 서버 발동·1초 복구,
  중복 거부·취소 후 재입력 제한·아이템 장착 거부도 확인했다.
- 로비에서 선택한 표정 ID 2가 보드 진입 후 4명 모두에게 동기화되었다.
  정상 감정표현 3종은 모든 상대에게 표시되었고 은신 중 감정표현은 상대 3명 모두
  표시되지 않았다. 4개 플레이어 로그에 예외 없음.
- 온라인 검증은 첫 감정표현을 실제 Input System T/마우스로, 나머지 조건은 서버 fixture로
  실행했다. 실제 네트워크 연결 끊김·재접속 전체 흐름은 이번 실행에서 재연하지 않았다.
- 정면/비스듬한 시점 렌더 검사에서 Tight sprite mesh가 일부 표정 획을 잘라내는 문제를
  발견하여 Full Rect로 수정하고 4종 모두 온전히 표시됨을 확인했다.
- 온라인 검증 이후 표정 미리보기 배경, 원형 중앙 안내/배경 UI 클릭 차단을 보완했다.
  마지막 프리팹 보완 후 EditMode 209/209 재통과. 표정 Full Rect 변경은 렌더로 검증했다.
- 검증 근거: 보조 폴더 `artifacts/expression-validation-2026-09-23/REPORT.md`,
  summary.json, 타임라인, UI 캡처 및 ExpressionPreview.png.
  검증 프로세스와 임시 Assets 코드를 모두 정리했다. 커밋·Notion 수정 없음.
